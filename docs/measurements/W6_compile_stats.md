# W6 플랜 컴파일 측정 (P2)

> docs/12 §10 의 산출물. **표는 실행 결과로 갱신한다. 손으로 추정치를 적지 않는다.**
> 아직 채워지지 않은 절은 어느 태스크가 채우는지 적어 둔다.

측정 환경은 `W1_env.md` 와 같다 (같은 기계 · 같은 키).

---

## 1. 프롬프트 프리픽스 (T2-04)

측정: `PromptPrefix.Build(masterdata)` · 토크나이저 `o200k_base` (Microsoft.ML.Tokenizers 2.0.0)

| 절 | 출처 | full | bare |
|---|---|---:|---:|
| `system_rules` | `masterdata/prompt/system_rules.md` | 905 | 905 |
| `action_catalog` | `actions.json` + `items.json` (플래그 표 · 레시피 입출력 포함) | 6,052 | 6,052 |
| `archetypes` | `archetypes.json` (아키타입별 허용 액션) | 2,392 | 2,392 |
| `dsl_summary` | `docs/03 §2` 요약 + 생성된 JSON Schema | 1,718 | 488 |
| `fewshot` | `masterdata/prompt/fewshot/*.json` 3건 | 900 | 900 |
| **합계** | | **11,967** | **10,737** |

- 하한 4,096 (프롬프트 캐시 최소 임계) **통과** — 2.9배.
- 상한 4,200~4,500 은 **W1 축소판 기준이었고 정식 카탈로그와 양립하지 않는다.**
  W1 프리픽스 4,409 tok 은 액션 12 · 플래그 16 짜리였다. 정식은 액션 37 · 플래그 42 이고,
  여기에 W1 에는 없던 것 셋이 더 붙는다:
  - 액션별 플래그 5종 표 (`V3.PRECONDITION_UNMET` 처방, docs/12 §8)
  - 레시피 37종 입출력 (`V3.RESOURCE_IMBALANCE` 처방)
  - 아키타입 40종 허용 액션 표 (`V2.ACTION_NOT_ALLOWED` 처방)
- 세 처방을 **서픽스가 아니라 프리픽스에 둔 이유**는 서픽스 예산이 300 토큰이기 때문이다.
  아키타입 허용 액션만 서픽스에 실어도 개별 재계획 요청이 300 을 넘긴다 (docs/12 §3).
- `docs/01 §10.1` 과 `CLAUDE.md §2.5` 의 예산을 이 실측으로 갱신했다 (T2-04, `TASKS.md §3`).

### 이 값이 뒤에 미치는 영향

| 대상 | 영향 |
|---|---|
| 외부 API 비용 | 캐시 적중분 단가로 요청당 약 $0.0003 (12k tok × $0.025/Mtok). 2,880 건이면 $0.9 수준 |
| 로컬 prefill | W1 8B 실측이 4.4k tok 콜드 2,608ms · 캐시 316ms 였다. 12k 는 콜드가 그 2.7배가 된다 — **T1 티어의 콜드 스타트가 P4 예산(docs/14 §3)에 걸리는지 T4-05 에서 다시 본다** |
| 캐시 임계 | 하한 대비 여유가 커서 카탈로그가 늘어도 임계 미달 위험은 없다 |

---

## 2. 디코딩 모드별 통과율 (T2-09 · T2-19)

### 2.1 프롬프트 모드 1차 실측 (T2-09, 2026-07-26)

`Compiler_ProducesValidJson` — 임의 버킷 20건(결정론 표본, stride 991), 재시도 없음.

| 엔진 | 디코딩 | 유효 JSON | 비고 |
|---|---|---:|---|
| `gemini-3.5-flash-lite` | prompt | **20/20** | 소요 30초. 1·2단 통과는 0 초과만 확인 |

W1 스파이크의 같은 모델이 94/100 이었던 것과 비교하면 프리픽스 보강(플래그 표·레시피·아키타입 표)이 유효 JSON 비율을 끌어올렸다. **다만 20건은 표본이 작다** — 확정은 T2-19 전량 회차다.

### 2.2 forced 모드는 아직 못 쟀다 — 무료 등급 일일 한도

**2026-07-26 실측: Gemini 무료 등급은 모델마다 하루 500건이다.**

```
quotaId : GenerateRequestsPerDayPerProjectPerModel-FreeTier
metric  : generativelanguage.googleapis.com/generate_content_free_tier_requests
limit   : 500  (model: gemini-3.1-flash-lite)
```

이날 `gemini-3.1-flash-lite`·`gemini-3.5-flash-lite`·`gemini-3.5-flash` 셋 다 429 로 막혔다(W1 스파이크가 이미 상당량을 썼다). 그래서 forced 모드 20건을 잇달아 돌리지 못했다.

**이것은 T2-19(2,880 버킷 전량 생성)의 전제를 바꾼다.** 무료 등급 하루치로는 한 모델당 500건이라 전량 1회차조차 하루에 끝나지 않는다. 선택지는 `TASKS.md §3` 의 결정 대기 항목에 적었다.

## 3. 통과율 — 전체 / 아키타입별 / 버킷 차원별 (T2-19 · T2-21)

*T2-19 · T2-21 에서 채운다.*

## 4. 실패 코드 분포 (개선 전/후) (T2-20 · T2-21)

*`W6_failures.md` 가 원자료이고, 여기에는 차수별 요약만 옮긴다.*

## 5. 재시도 횟수별 누적 성공률 (T2-19)

*T2-19 에서 채운다.*

## 6. 모델별 통과율 · 요청당 지연 · 토큰 · 비용 (T2-19)

*T2-19 에서 채운다. 429 최초 발생 동시성도 여기에 적는다.*

## 7. 플랜 다양성 지표 (T2-22)

*T2-22 에서 채운다. 목표 ≥ 60%.*

## 8. P2 게이트 판정 (T2-24)

*T2-24 에서 채운다.*
