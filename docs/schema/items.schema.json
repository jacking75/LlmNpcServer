{
  "type": [
    "object",
    "null"
  ],
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "type": [
          "object",
          "null"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "문자열 id. 다른 파일이 이 값으로 참조한다 (V3)."
          },
          "code": {
            "type": "integer",
            "description": "파일 내 유일해야 한다 (V1). **재배치하지 않는다** — 프리베이크된 플랜이 깨진다."
          },
          "category": {
            "type": "string"
          },
          "grants": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": [
                "string",
                "null"
              ],
              "enum": [
                "AllyNearby",
                "AtField",
                "AtGate",
                "AtHome",
                "AtMarket",
                "AtTavern",
                "AtTemple",
                "AtWorkplace",
                "GatesOpen",
                "HasCoin",
                "HasCompanion",
                "HasCustomer",
                "HasFood",
                "HasProduct",
                "HasRawMaterial",
                "HasTool",
                "HasWater",
                "HasWeapon",
                "InCombat",
                "InWilderness",
                "InventoryFull",
                "IsAlone",
                "IsDawn",
                "IsDay",
                "IsEvening",
                "IsExhausted",
                "IsHungry",
                "IsInjured",
                "IsNight",
                "IsRested",
                "IsSleeping",
                "IsThirsty",
                "MarketOpen",
                "OnDuty",
                "PathBlocked",
                "PlayerNearby",
                "RegionPeaceful",
                "RegionUnderAttack",
                "ResourceDepleted",
                "ShopOpen",
                "ThreatNearby",
                "WeatherHarsh"
              ]
            }
          },
          "stack": {
            "type": "integer"
          },
          "_comment": {
            "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
          }
        }
      }
    },
    "recipes": {
      "type": [
        "array",
        "null"
      ],
      "items": {
        "type": [
          "object",
          "null"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "문자열 id. 다른 파일이 이 값으로 참조한다 (V3)."
          },
          "workplace_type": {
            "type": "string"
          },
          "inputs": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": [
                "object",
                "null"
              ],
              "properties": {
                "item": {
                  "type": "string"
                },
                "count": {
                  "type": "integer"
                },
                "_comment": {
                  "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
                }
              }
            }
          },
          "outputs": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "$ref": "#/properties/recipes/items/properties/inputs/items"
            }
          },
          "duration_s": {
            "type": "integer"
          },
          "_comment": {
            "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
          }
        }
      }
    },
    "_comment": {
      "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
    },
    "$schema": {
      "description": "에디터용 스키마 경로. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다.",
      "type": "string"
    },
    "version": {
      "description": "파일 형식 버전. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다.",
      "type": "integer"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "아이템·레시피",
  "$comment": "content_hash=27edd9b5ede3e80249a51f6c26b643691e98ac0854475f92100fc7fa9a0762f1 기준. 허용 값은 이 마스터데이터 상태의 것이다.",
  "description": "items.json 의 구조. **스키마는 검증기가 아니다** — 교차 제약(V5 가중치 합 · V6 total_keys · V10 정원 · V11 도달)은 여기서 표현할 수 없고 MasterDataValidator 가 판정한다. docs/reference_masterdata.html 이 전문이다."
}
