{
  "type": [
    "object",
    "null"
  ],
  "properties": {
    "version": {
      "description": "파일 형식 버전. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다.",
      "type": "integer"
    },
    "reserved_bits": {
      "type": "array",
      "items": {
        "type": "integer"
      }
    },
    "flags": {
      "type": "array",
      "items": {
        "type": [
          "object",
          "null"
        ],
        "properties": {
          "bit": {
            "type": "integer",
            "description": "0..63. **재배치하지 않는다.** 추가는 reserved_bits 안에서만 (V1)."
          },
          "id": {
            "type": "string",
            "description": "문자열 id. 다른 파일이 이 값으로 참조한다 (V3)."
          },
          "group": {
            "type": "string"
          },
          "desc": {
            "type": "string"
          },
          "_comment": {
            "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
          }
        }
      }
    },
    "_comment": {
      "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
    },
    "$schema": {
      "description": "에디터용 스키마 경로. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다.",
      "type": "string"
    },
    "exclusive_groups": {
      "description": "설명용 필드. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "월드 플래그 — bit 번호는 재배치하지 않는다",
  "$comment": "content_hash=a78156a66e978f625fc5a1454a106f67d10c33b4ef1b5b1a4e5f0147cb8b783d 기준. 허용 값은 이 마스터데이터 상태의 것이다.",
  "description": "world_flags.json 의 구조. **스키마는 검증기가 아니다** — 교차 제약(V5 가중치 합 · V6 total_keys · V10 정원 · V11 도달)은 여기서 표현할 수 없고 MasterDataValidator 가 판정한다. docs/reference_masterdata.html 이 전문이다."
}
