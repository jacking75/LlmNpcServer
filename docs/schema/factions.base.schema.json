{
  "type": [
    "object",
    "null"
  ],
  "properties": {
    "version": {
      "description": "파일 형식 버전. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다.",
      "type": "integer"
    },
    "factions": {
      "type": "array",
      "items": {
        "type": [
          "object",
          "null"
        ],
        "properties": {
          "id": {
            "type": "string",
            "description": "문자열 id. 다른 파일이 이 값으로 참조한다 (V3)."
          },
          "code": {
            "type": "integer",
            "description": "파일 내 유일해야 한다 (V1). **재배치하지 않는다** — 프리베이크된 플랜이 깨진다."
          },
          "desc": {
            "type": [
              "string",
              "null"
            ]
          },
          "hostile_to": {
            "type": [
              "array",
              "null"
            ],
            "items": {
              "type": [
                "string",
                "null"
              ]
            },
            "description": "적대 세력. **NPC 서버는 판정에 쓰지 않는다** — 적대 판정은 게임서버 몫이다 (B-06)."
          },
          "_comment": {
            "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
          }
        }
      }
    },
    "_comment": {
      "description": "사람이 읽는 주석. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다."
    },
    "$schema": {
      "description": "에디터용 스키마 경로. **로더는 읽지 않는다** — 값을 고쳐도 동작이 바뀌지 않는다.",
      "type": "string"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "세력 — code 는 재배치하지 않는다",
  "$comment": "정적 변형 — 허용 값(enum)이 없다. 구조만 설명한다.",
  "description": "factions.json 의 구조. **스키마는 검증기가 아니다** — 교차 제약(V5 가중치 합 · V6 total_keys · V10 정원 · V11 도달)은 여기서 표현할 수 없고 MasterDataValidator 가 판정한다. docs/reference_masterdata.html 이 전문이다."
}
