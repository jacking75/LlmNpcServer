# LLM 기반 MMORPG NPC 서버 — 타당성 판단 및 개발 계획

작성일: 2026-07-25 (v3 — 결과물 정의·게임서버 연동·마스터데이터 반영, §13~§15 추가 및 상세 문서 분리)
전제: Windows / .NET 10 / 로컬 LLM(dotLLM) + 외부 OpenAI 호환 API 병용 / 소비자용 GPU(VRAM 12~32GB) / NPC 5,000 / 단일 월드 / 사내 R&D 및 기술 검증

> **이 문서는 상위 계획서다.** 실제 구현에 쓸 세부 사양은 [`docs/`](docs/) 아래에 분리되어 있다. 문서 맵은 §16.

---

## 1. 결론 요약

| 질문 | 답 |
|---|---|
| 만들 가치가 있는가 | **있다.** 단, 가치의 원천은 "런타임 지능"이 아니라 **행동 콘텐츠 오써링 자동화**다. |
| 만들 수 있는가 | **로컬 GPU 단독으로는 원안이 불가능하다.** 다만 **외부 API를 병용하면 원안도 처리량·비용 모두 성립한다** (§10). 그럼에도 캐시 구조를 권장하는 이유는 비용이 아니라 **가치와 운영 안정성** 때문이다. |
| 대안 | 선택한 **하이브리드 구조** + **플랜 캐시(아키타입 × 상황 버킷)** + **3-티어 라우팅(캐시 / 로컬 / 외부)**. |
| dotLLM 채택 | **R&D용으로는 적합하다.** 반드시 `Microsoft.Extensions.AI.IChatClient` 뒤로 추상화해서 외부 API와 무코드 교체·스필오버가 되게 둔다. |
| 시분할(200개씩) | **방향은 맞다.** 다만 이득의 출처가 "나눠서"가 아니라 **"고를 수 있어서"** 이고, 로컬에서 요청 패킹의 이득은 정확히 0이다 (§11). |
| 최대 리스크 | 기술이 아니라 **"플레이어가 차이를 인지하는가"** 다. 이 검증을 로드맵 끝이 아니라 중간에 배치해야 한다. |

---

## 2. 원안의 문제점

원안: *"로컬 LLM이 모든 NPC의 동작을 계획하고, 상태를 주기적으로 파악해서 행동 루틴을 결정한다."*

### 2.1 처리량이 성립하지 않는다

dotLLM은 현재 **continuous batching이 미구현**(Phase 9 로드맵)이다. 즉 실질적으로 단일 스트림 서빙이다. 저자 본인도 "프로덕션에서 llama.cpp나 vLLM을 대체할 물건은 아니다 — 적어도 아직은"이라고 명시했다.

8B Q4_K, RTX 4090/5090급 기준 추정치와 **W1 실측**(RTX 4060 · VRAM 8GB · `docs/measurements/W1_results.md`):

| 항목 | 토큰 | 처리 속도 | 추정 | **W1 실측 (8B)** |
|---|---|---|---|---|
| 고정 프리픽스 (시스템 규칙 + 액션 카탈로그 + DSL 스펙 + few-shot) | ~1,500 → **실측 11,967** | prompt cache 적중 | ≈ 0 | ≈ 0 (적중 시) |
| 가변 서픽스 (NPC 상태 스냅샷) | ~250 | prefill ~1,000 tok/s | 0.25s | **0.32s** |
| 출력 (플랜 JSON) | ~150 → **실측 326** | decode ~100 tok/s | 1.5s | **4.83s** (44 tok/s) |
| **합계** | | | **≈ 1.75s** | **5.14s** (4B 3.70s) |

> dotLLM의 decode는 llama.cpp의 0.8x 수준이지만 **prefill은 2~5배 느리다.** 플랜 생성은 "긴 입력 → 짧은 출력" 형태라 prefill 비중이 큰 작업이므로 이 약점이 직격한다. 대응책은 아래 §4.2의 프리픽스 고정 + 서픽스 최소화다.

**W1 판정 — 대응책은 맞았고 지연 가정은 틀렸다.** 프리픽스 캐시는 prefill을 실제로 **85~88% 줄였고**(4B 1,531→237ms · 8B 2,608→316ms), 그래서 prefill은 가정한 0.25s에 붙는다. 빗나간 것은 **decode**다 — 가정 100 tok/s에 실측 44 tok/s(8B)·66 tok/s(4B)라 총 지연이 2.1~2.9배가 됐다. 즉 **"prefill이 병목"이라는 §4.2의 전제는 캐시를 켠 뒤에는 더 이상 참이 아니다.**

→ 추정 **0.57 req/s = 2,050 req/hour**, **W1 실측 8B 0.19 req/s = 700 req/hour** (동시 1 · GPU 100% 점유 기준)

동시성을 올려도 8B는 나아지지 않는다 — 동시 2에서 2.15 req/s로 올랐다가 **동시 8에서 0.36 req/s로 붕괴한다.** 배칭 부재가 아니라 **VRAM 8GB에서 KV 캐시가 넘쳐 PCIe 페이징이 걸리기 때문이다**(`W1_env.md §1`). 4B는 붕괴하지 않고 동시 2 이상에서 약 12 req/s로 평평하다.

NPC 5,000마리가 게임 시간 1시간마다 재계획하면 5,000 req/hour가 필요하다. **추정 용량으로도 2.4배 초과였고, 실측 8B 용량으로는 7.1배 초과다.** 4시간에 한 번으로 낮춰도 1,250 req/hour라 실측 용량을 여전히 넘는다. **결론의 방향은 실측이 강화했다** — 로컬 단독으로는 성립하지 않는다.

### 2.2 설계 문제이기도 하다

플레이어가 동시에 인지하는 NPC는 20~50마리 수준이다. 5,000마리 전원에게 개별 지능을 부여해도 **관측되지 않는 연산**이 대부분이다. 지능 예산은 플레이어 인지 범위에 집중되어야 한다 — 렌더링 LOD와 같은 원리를 인지(cognition)에 적용하는 것이다.

### 2.3 "장시간 실행 컨텍스트 관리" 문제는 회피 가능하다

원안에서 우려한 부분인데, 이건 해결할 문제가 아니라 **설계로 제거할 문제**다. 상세는 §4.2.

---

## 3. 권장 아키텍처

핵심 발상: **LLM을 실행기가 아니라 컴파일러로 쓴다.** LLM은 행동 플랜을 *생성*하고, 결정론적 런타임이 그것을 *실행*한다. 생성은 느리고 비싸도 되지만 실행은 빠르고 재현 가능해야 한다.

```
┌──────────────────────────────────────────────────────────┐
│ L0  결정론적 NPC 런타임        (틱 기반, LLM 무관)        │
│     · 플랜 실행기 (BT / HTN 하이브리드)                    │
│     · 인지 LOD: 근거리=full tick, 원거리=압축 시뮬        │
│     · 수천 NPC를 SoA 레이아웃으로 tick                     │
└───────────────▲──────────────────────────────────────────┘
                │ 플랜 조회 (O(1), 락 프리 스왑)
┌───────────────┴──────────────────────────────────────────┐
│ L1  플랜 스토어 / 캐시                                     │
│     key = (archetype, context_bucket)                     │
│     + (npc_id) 개별 오버라이드                             │
│     · 버전·TTL·히트율 메트릭                               │
└───────────────▲──────────────────────────────────────────┘
                │ 캐시 미스 / 무효화 시에만
┌───────────────┴──────────────────────────────────────────┐
│ L2  재계획 트리거 + 요청 큐 (Channels, 우선순위)           │
│     · 시간이 아니라 **사건**이 재계획을 유발               │
│     · 디바운스 + 전역 토큰 버짓 레이트리미터               │
│     · 게임 틱을 절대 블록하지 않음 (완전 비동기)           │
└───────────────▲──────────────────────────────────────────┘
                │
┌───────────────┴──────────────────────────────────────────┐
│ L3  플랜 컴파일러 (LLM)                                    │
│     · 무상태 호출, JSON Schema 강제 디코딩                 │
│     · 4단 검증기 → 실패 시 폴백 플랜                       │
│     · IChatClient 추상화 (dotLLM / Ollama / llama.cpp)     │
└──────────────────────────────────────────────────────────┘
```

### 3.1 플랜 캐시가 처리량 문제를 푼다

개별 NPC 단위로 계획하지 않고 **(아키타입 × 상황 버킷)** 단위로 계획한다.

- 아키타입: 대장장이, 경비병, 상인, 농부, 사제, 어부, … ≈ 40종
- 상황 버킷: 시간대(6) × 지역상태(4: 평시/경계/전시/재해) × 계절·날씨(3) = 72

→ 유니크 키 **40 × 72 = 2,880개**

| 국면 | 비용 |
|---|---|
| 콜드 필 (전체 2,880키 생성) | 2,880 ÷ 0.57 req/s ≈ **84분** — 1회성, 빌드 파이프라인에서 오프라인 프리베이크 가능 |
| 정상 상태 | 무효화된 버킷만 재생성 → 시간당 수십 건 |
| 남는 GPU 예산 | **전량을 개별 NPC 이벤트 재계획에 투입** (≈ 2,000 req/hour = 34 req/min) |

플레이어 근처의 "주목받는" NPC 수십 마리에게 개별 계획을 몰아주기에 충분한 예산이다. **캐시 히트율 98% 이상**이 R&D 성공 기준이 된다.

### 3.2 플랜 DSL

LLM 출력은 자연어가 아니라 스키마가 강제된 JSON이어야 한다. dotLLM의 FSM/PDA 기반 logit masking(JSON Schema / GBNF)이 이 지점에서 핵심 기능이다.

```json
{
  "plan_id": "blacksmith@evening.siege.winter",
  "goal": "restock_ore",
  "valid_until": { "game_time": "22:00", "invalidate_on": ["region_state_change"] },
  "steps": [
    { "action": "MoveTo",  "args": { "poi": "mine_entrance" }, "timeout_s": 300 },
    { "action": "Gather",  "args": { "resource": "iron_ore", "count": 10 }, "timeout_s": 900 },
    { "action": "MoveTo",  "args": { "poi": "smithy" } },
    { "action": "Work",    "args": { "recipe": "iron_sword", "count": 2 } },
    { "action": "Sleep",   "args": { "poi": "home_bed" } }
  ],
  "interrupts": [
    { "when": "combat_nearby",  "then": "flee_to:town_gate" },
    { "when": "player_interact","then": "yield_to_dialogue" },
    { "when": "hp_below_30",    "then": "goto_step:0" }
  ],
  "fallback": "default_blacksmith_routine"
}
```

설계 제약 (소형 모델의 계획 능력 한계를 고려한 것):

- 액션 카탈로그는 **40개 이하**로 유지한다. 각 액션은 전제조건·효과·비용을 명시한다.
- 플랜 길이는 **10스텝 이하**로 제한한다.
- 인터럽트는 LLM이 아니라 **런타임이 처리**한다. LLM은 "무엇을 할지"만 정하고 "위험할 때 어떻게 할지"는 결정론 규칙에 맡긴다.
- few-shot은 3개 이내로 넣는다. 그 이상은 프리픽스 길이만 늘리고 품질 이득이 적다.

### 3.3 4단 검증기 (필수)

LLM 출력은 신뢰하지 않는다. 검증 통과 전에는 런타임에 절대 주입하지 않는다.

1. **스키마** — JSON Schema 재검증 (강제 디코딩이 있어도 이중 확인)
2. **어휘** — 모든 action / poi / resource ID가 실제 카탈로그에 존재하는가
3. **정합성** — 각 스텝의 전제조건이 앞 스텝의 효과로 충족되는가 (GOAP 스타일 정적 검사), 도달 불가능한 POI는 없는가
4. **드라이런** — 헤드리스 시뮬레이터에서 압축 시간으로 1회 실행. 데드락·무한루프·자원 고갈 검출

실패하면 즉시 **폴백 플랜**(손으로 짠 기본 루틴)으로 대체하고, 실패 케이스를 로그에 적재한다. 검증 실패율은 3% 이하가 목표다.

---

## 4. 컨텍스트 관리 — 문제 자체를 없앤다

원안에서 "장시간 실행되므로 LLM이 컨텍스트 관리를 잘 해야 한다"고 했는데, 이 문제는 **모든 LLM 호출을 무상태로 만들면 사라진다.**

### 4.1 NPC별 대화 이력을 유지하지 않는다

NPC의 기억은 LLM 컨텍스트가 아니라 **게임 DB의 구조화된 데이터**에 있다.

| 항목 | 저장 위치 | 형태 |
|---|---|---|
| 최근 사건 | DB, 링 버퍼 N=8 | `{type, actor, when, salience}` 구조체 |
| 관계 | DB | `npc_id → {target, affinity: -100..100}` |
| 목표 스택 | DB | 열거형 + 파라미터 |
| 진행 중 플랜 | 메모리 | 플랜 ID + 스텝 인덱스 |

요약(summarization)은 LLM이 아니라 **규칙**으로 한다. `salience` 점수 상위 8개만 남기고 버리는 식이다. 이러면 컨텍스트 드리프트, 요약 누적 오염, 무한히 자라는 히스토리 문제가 원천적으로 발생하지 않는다.

### 4.2 프롬프트 구조 — 프리픽스 고정, 서픽스 최소화

```
[고정 프리픽스 · 전 요청 공유 · 약 1,500 토큰]      ← prompt cache 적중
  시스템 규칙
  액션 카탈로그 (전제조건/효과)
  플랜 DSL 스펙
  few-shot 3개

[가변 서픽스 · 250 토큰 이하]                        ← 매 요청 prefill
  { "archetype": "blacksmith", "time": "evening",
    "region": "siege", "season": "winter",
    "inventory": {"iron_ore": 0, "coal": 3},
    "recent": ["mine_blocked", "order_from_player"],
    "affinity_top": [{"guard_captain": 40}] }
```

dotLLM의 prefill이 느리다는 약점 때문에 이 분리는 선택이 아니라 필수다. 프리픽스는 **바이트 단위로 완전히 동일**해야 캐시에 적중하므로, 프롬프트 조립 코드에서 프리픽스를 상수 문자열로 고정하고 동적 값이 섞이지 않도록 테스트로 강제한다.

### 4.3 보안: 플레이어 입력은 프롬프트에 넣지 않는다

플레이어 캐릭터명, 채팅, 길드명 등 **플레이어가 작성한 문자열은 어떤 경로로도 플래너 프롬프트에 원문으로 들어가면 안 된다.** 프롬프트 인젝션으로 NPC 행동을 조작하는 익스플로잇이 성립한다. 이벤트는 구조화된 enum 코드로만 표현하고, 이름이 필요하면 `player_7f3a` 같은 내부 ID로 치환한다.

---

## 5. .NET 10 구현 설계

### 5.1 프로젝트 구조

```
Npc.Contracts   게임서버 연동 인터페이스 + 패킷 DTO         (순수 · 의존성 0)
Npc.Core        액션 카탈로그, 플랜 DSL 모델, 검증기         (순수 · 의존성 0)
Npc.MasterData  마스터데이터 로더 · 검증 · 읽기전용 인덱스
Npc.Runtime     틱 스케줄러, 플랜 실행기, 인지 LOD, 월드 뷰
Npc.Planning    플랜 캐시, 컨텍스트 버킷터, 우선순위 재계획 큐
Npc.Llm         IChatClient 어댑터, 프롬프트 조립, 3-티어 라우터, 토큰 예산
Npc.Gateway     IGameServerLink 구현체 (Loopback / Null / Recording / Replay)
Npc.Sim         헤드리스 월드 시뮬레이터 = 게임서버 대역
Npc.Host        ASP.NET 호스트, 메트릭, 대시보드
Npc.Tests       골든 시나리오 회귀 테스트, 결정론 리플레이, 장애 주입
```

`Npc.Sim`을 초반에 만드는 것이 중요하다. 실제 게임 클라이언트 없이 검증 사이클을 돌릴 수 있어야 R&D 속도가 나온다.

`Npc.Contracts`와 `Npc.Core`는 **외부 NuGet 의존이 0**이어야 한다. 순수 로직만 담아서 테스트가 빠르고 미래 이식이 자유롭게 한다.

### 5.2 기술 선택

| 영역 | 선택 | 이유 |
|---|---|---|
| LLM 추상화 | `Microsoft.Extensions.AI` 의 `IChatClient` | dotLLM(OpenAI 호환 엔드포인트) / Ollama / llama.cpp-server 를 무코드 교체. 대조군 실험 필수 |
| LLM 연결 | **별도 프로세스 + HTTP** (in-proc NuGet 아님) | GPL 경계 분리, GC 격리, 크래시 격리, 교체 용이 |
| 요청 큐 | `System.Threading.Channels` (Bounded, 우선순위 2단) | 백프레셔 자연 처리. 큐가 차면 저우선순위 요청 드롭 |
| 워커 | `BackgroundService` × 1~2 | 배칭이 없으므로 워커를 늘려도 이득 없음. 오히려 GPU 컨텐션 |
| 직렬화 | `System.Text.Json` source generator | 리플렉션 제거, 틱 루프에서 할당 최소화 |
| NPC 상태 | SoA(struct of arrays) + `ArrayPool<T>` | 수천 NPC 틱에서 캐시 지역성과 GC 압력이 곧 성능 |
| 관측 | OpenTelemetry + `Meter` | 캐시 히트율, 검증 실패율, 큐 깊이, 요청 지연을 상시 계측 |

### 5.3 GC 격리가 프로세스 분리의 숨은 이유

추론 엔진을 같은 프로세스에 넣으면 대용량 텐서 버퍼가 게임 서버의 GC 특성을 오염시킨다. 프로세스를 분리하면 NPC 런타임은 Server GC + 낮은 지연 튜닝을, 추론 프로세스는 dotLLM의 sustained low-latency GC 모드를 각각 독립적으로 쓸 수 있다.

### 5.4 모델 후보 (Phase 0에서 실측 비교)

| 모델 | 크기 | 라이선스 | 메모 |
|---|---|---|---|
| Qwen3 8B | 8B | Apache 2.0 | 기본 후보. 구조화 출력·함수 호출 지원, 크기 대비 추론력 양호 |
| Qwen3 4B | 4B | Apache 2.0 | 처리량 2배. 액션 카탈로그를 충분히 줄이면 이 정도로도 될 가능성 |
| Phi-4-mini | 3.8B | MIT | 라이선스 가장 자유로움. 최저 지연 시나리오 대조군 |
| Gemma 3 12B | 12B | Gemma Terms | 품질 상한 확인용. VRAM 24GB 필요 |

**중요한 검증 포인트:** 4B와 8B의 플랜 품질 차이가 유의미하지 않다면 4B를 쓰는 것이 정답이다. 처리량이 2배가 되고, 그만큼 개별 NPC 재계획 예산이 늘어난다. Phase 0에서 이걸 먼저 재야 한다.

---

## 6. 로드맵 (약 12주)

| 주차 | 산출물 | 게이트 | 상세 |
|---|---|---|---|
| **W1** | **스파이크.** dotLLM `serve` 기동, JSON Schema 강제 출력 확인, prefill/decode 실측, 모델 4종 비교 | 1.75s/요청 가정이 실측과 ±50% 이내인가. 아니면 즉시 재설계 | [docs/10](docs/10_Phase0_W1_Spike.md) |
| **W2–4** | `Npc.Core` + `Npc.Runtime` + `Npc.Sim` + 마스터데이터. **LLM 없이** 폴백 플랜 40종으로 구동 | LLM 0회 호출로 NPC 500마리가 게임 7일을 완주한다 | [docs/11](docs/11_Phase1_W2-4_Core_Runtime.md) |
| **W5–6** | `Npc.Llm` 플랜 컴파일러 + 4단 검증기 + 폴백. 캐시 없이 단건 생성 | 검증 통과율 ≥ 90% | [docs/12](docs/12_Phase2_W5-6_Plan_Compiler.md) |
| **W7–8** | 플랜 캐시 + 버킷 인덱싱 + 프리베이크 CLI + 사람 검수 | 2,880키 콜드 필 완주, 히트율 ≥ 98% | [docs/13](docs/13_Phase3_W7-8_Plan_Cache.md) |
| **W9–10** | 우선순위 재계획 큐 + 3-티어 라우팅 + 인지 LOD. **NPC 5,000 부하 테스트** | 히트율 ≥ 98%, GPU ≤ 60%, 틱 p99 ≤ 20ms | [docs/14](docs/14_Phase4_W9-10_Scheduler_Tiering.md) |
| **W11** | 골든 회귀 + 결정론 리플레이 + 장애 주입 + 링크 교체 검증 | 시나리오 A/B/C 통과, 리플레이 100% 일치 | [docs/15](docs/15_Phase5_W11-12_Verification.md) |
| **W12** | **블라인드 평가** + 오써링 공수 정산 + 결과 보고 | 아래 §7 | [docs/15](docs/15_Phase5_W11-12_Verification.md) |

W2–4에서 LLM을 일부러 배제하는 것이 중요하다. LLM이 없어도 동작하는 런타임이 먼저 있어야 LLM의 기여분을 측정할 수 있고, 폴백 경로가 자동으로 확보된다.

---

## 7. 성공 기준 — 그리고 진짜 리스크

기술 지표는 사실 달성 가능성이 높다. 이 프로젝트의 실질적 게이트는 다음 하나다.

> **블라인드 A/B: 관찰자가 "LLM이 계획한 NPC"와 "기획자가 손으로 짠 BT NPC"를 구분할 수 있는가.**

구분하지 못한다면 — 그리고 이 결과가 나올 확률은 결코 낮지 않다 — 결론은 "실패"가 아니라 **가치의 위치를 바꾸는 것**이다.

### 가치는 런타임이 아니라 오써링에 있다

기존 방식에서 NPC 40 아키타입 × 72 상황의 행동 루틴을 기획자가 손으로 짜면 수천 개 조합을 사람이 채워야 한다. 현실적으로 아무도 안 하고, 그래서 대부분의 MMORPG NPC는 상황과 무관하게 같은 루틴을 돈다.

LLM 플랜 컴파일러는 그 2,880개를 **자동으로 채운다.** 그리고 그 결과물은 사람이 읽고 수정할 수 있는 JSON 플랜이다. 즉:

- 기획자는 플랜을 **작성**하는 대신 **검수·수정**한다
- 생성 결과가 마음에 안 들면 그 플랜만 손으로 고치고 캐시에 고정(pin)한다
- 신규 지역/시즌 추가 시 프리베이크만 다시 돌리면 된다

이렇게 프레이밍하면 **런타임에 LLM이 없어도 가치가 성립한다.** 전량 오프라인 프리베이크로 돌려도 콘텐츠 제작 비용이 줄어든다. 런타임 LLM 호출은 그 위에 얹는 옵션이 된다.

R&D 보고서의 결론은 "NPC가 똑똑해졌다"가 아니라 **"행동 콘텐츠 제작 공수가 N% 줄었고, 그중 M%는 기획자 수정 없이 채택 가능했다"** 가 되어야 설득력이 있다.

---

## 8. 리스크 대장

| # | 리스크 | 영향 | 대응 |
|---|---|---|---|
| R1 | **인지 불가** — 플레이어가 차이를 못 느낌 | 프로젝트 가치 소멸 | 블라인드 평가를 W12가 아니라 **W8에 예비 실시**. 가치 축을 오써링 자동화로 재정의 |
| R2 | **dotLLM 성숙도** — v0.1.0-preview, 배칭 없음, prefill 2~5배 느림, 저자가 프로덕션 부적합 명시 | 처리량·안정성 | `IChatClient` 추상화. W1에 Ollama/llama.cpp-server 대조군 동시 구축. 프리픽스 캐시로 prefill 회피 |
| R3 | **GPLv3** | 상용 전환 시 법적 제약 | **별도 프로세스 + HTTP 경계**로만 사용. NuGet in-proc 임베딩 금지. 배포하지 않으면 GPL 의무가 발동하지 않지만, 상용 전환을 고려한다면 W1 시점에 법무 확인을 걸어둔다 |
| R4 | **소형 모델 계획 품질** | 검증 실패율 상승 | 액션 ≤40, 스텝 ≤10, 스키마 강제, 폴백 상시 준비. 품질 미달 시 아키타입 수를 줄이고 파라미터화로 커버 |
| R5 | **비결정성 → 버그 재현 불가** | 운영 부담 | 플랜은 생성 시점에 확정되어 **저장**된다. 버그 재현은 저장된 플랜 + 시드로 LLM 없이 가능. 이것이 "컴파일러 방식"의 핵심 이점 |
| R6 | **프롬프트 인젝션** | 익스플로잇 | 플레이어 작성 문자열은 프롬프트에 절대 미투입. 구조화 enum만 사용 (§4.3) |
| R7 | **GPU 단일 장애점** | 재계획 전면 중단 | 폴백 플랜으로 게임은 계속 동작해야 한다. LLM 없이도 NPC가 정상 작동하는 것이 W2–4의 게이트인 이유 |

---

## 9. 지금 바로 할 것 (W1 스파이크 체크리스트)

1. dotLLM 릴리스 바이너리로 `serve` 기동 → `/v1/chat/completions` 확인
2. `Microsoft.Extensions.AI` + OpenAI 클라이언트를 `http://localhost:*` 로 연결 → `IChatClient` 한 줄로 붙는지 확인
3. 플랜 DSL JSON Schema 작성 → 강제 디코딩으로 **항상 유효한 JSON**이 나오는지 100회 반복 검증
4. 고정 프리픽스 1,500토큰 + 가변 서픽스 250토큰 구성 → **prompt cache 적중 시 prefill 시간 감소폭 실측**
5. Qwen3 4B / Qwen3 8B / Phi-4-mini Q4_K 3종의 요청당 지연 + 플랜 품질(수동 평가 20건) 비교
6. 같은 실험을 Ollama로 반복 → dotLLM 대비 처리량 격차 기록

이 6개 결과가 나오면 §2.1의 산술을 실측치로 갱신하고, 캐시 키 개수와 아키타입 수를 그에 맞춰 확정한다.

---

---
---

# v2 개정 — 외부 API 병용 및 시분할 스케줄링

## 10. 외부 OpenAI 호환 API 병용

### 10.1 외부 API의 진짜 레버는 품질이 아니라 **동시성**이다

dotLLM의 결정적 약점은 continuous batching 부재다. 그런데 상용 API 서비스는 **서버 측에 continuous batching이 이미 들어있다.** 즉 클라이언트가 동시 요청을 늘리면 처리량이 거의 선형으로 늘어난다. 이 하나가 판을 바꾼다.

요청당 지연 2초 가정:

| 구성 | 처리량 | 프리베이크 2,880건 소요 |
|---|---|---|
| 로컬 dotLLM (단일 스트림) | 2,057 req/h | **84분** |
| 외부 API 동시 8 | 14,400 req/h | 720초 |
| 외부 API 동시 16 | 28,800 req/h | 360초 |
| **외부 API 동시 32** | **57,600 req/h** | **180초 (3분)** |
| 외부 API 동시 64 | 115,200 req/h | 90초 |

동시 32만 써도 로컬 대비 **28배**다. 프리베이크가 84분에서 3분이 된다 — 개발 반복 주기가 완전히 달라진다.

> **이 표는 추정이고, 동시 32·64는 아직 재지 않았다** (`W1_results.md` M4). W1은 로컬 2종만 1/2/4/8로 쟀다.
> 실측된 것은 P3 파일럿뿐이다 — **동시 24까지 429가 한 번도 나지 않았고**, 288버킷을 115초에 채웠다
> (`W8_prebake.md §4`). 전량 2,880 외삽은 **12~19분**이라 이 표의 3분과 4~6배 차이가 난다.
> 요청당 지연 2초 가정이 실측 프리픽스(11,967 tok)에서 성립하지 않기 때문이다.
>
> **근거가 없는 32를 기본값으로 두지 않는다** — `--concurrency` 기본은 **8**이다(`docs/13 §4`).
> 올리려면 429가 처음 나는 지점을 먼저 재고 그 절반을 쓴다(`AdaptiveConcurrency.RecommendedStart`).
>
> **다만 §10.1의 주장 자체는 실측이 지지한다.** 로컬 8B는 동시 8에서 오히려 **붕괴하는데**(0.36 req/s)
> 외부는 동시 24까지 429 없이 선형에 가깝게 올라갔다. **"외부의 레버는 품질이 아니라 동시성"은 맞았고,
> 틀린 것은 배수다.**

### 10.2 비용 (단일 월드 NPC 5,000 기준)

요청당 입력 1,750토큰(고정 프리픽스 1,500 + 가변 서픽스 250), 출력 150토큰. 프롬프트 캐시 적용 시 프리픽스는 10% 과금(3사 모두 90% 할인).

> **⚠ 아래 세 표는 전부 이 가정 위에 서 있고, 그 가정이 8배 틀렸다.** 실측은 입력 **13,948 tok** · 출력 **326 tok**이다
> (`docs/measurements/cost_actual.md`). 프리픽스 1,500은 W1 축소판(액션 12·플래그 16, 4,409 tok) 값이었고
> 정식 카탈로그는 **11,967 tok**이다. 실측 대조는 §10.2.1에 있다. **표를 인용하기 전에 그쪽을 먼저 본다.**

**시나리오 A — 프리베이크 2,880건 (1회성)**

| 모델 | 캐시 적용 | 미적용 |
|---|---|---|
| GPT-5 nano | **$0.23** | $0.42 |
| Qwen3.5 Flash | $0.29 | $0.68 |
| Gemini 3.1 Flash-Lite | $0.94 | $1.91 |
| GPT-5 mini | $1.15 | $2.12 |
| Claude Haiku 4.5 | $3.31 | $7.20 |

**시나리오 B — 이벤트 재계획 20,000건/일** (NPC당 하루 4회)

| 모델 | 일 | 월 |
|---|---|---|
| GPT-5 nano | $1.60 | **$48** |
| Qwen3.5 Flash | $2.00 | $60 |
| Gemini 3.1 Flash-Lite | $6.50 | $195 |
| GPT-5 mini | $8.00 | $240 |
| Claude Haiku 4.5 | $23.00 | $690 |

**시나리오 C — 원안 그대로, 전면 재계획 120,000건/일** (NPC당 하루 24회)

| 모델 | 일 | 월 |
|---|---|---|
| GPT-5 nano | $9.60 | **$288** |
| Qwen3.5 Flash | $12.00 | $360 |
| Gemini 3.1 Flash-Lite | $39.00 | $1,170 |
| GPT-5 mini | $48.00 | $1,440 |
| Claude Haiku 4.5 | $138.00 | $4,140 |

> 가격은 2026년 7월 기준 공개 단가이며 변동한다. Phase 0에서 실단가로 갱신한다.

### 10.2.1 실측 대조 (T0-13 · W12 정산)

**W1(P0)에서는 못 냈다** — 로컬 2종만 쟀고 `W1_perf.csv`의 `CostUsd`가 180행 전부 0이다. 실비용은 P3·P5 회차에서 나왔다.

| 항목 | §10.2 가정 | 실측 | 배수 |
|---|---|---|---|
| 입력 토큰 / 요청 | 1,750 | **13,948** | **8.0×** |
| 출력 토큰 / 요청 | 150 | 326 | 2.2× |
| 프롬프트 캐시 적중률 | 사실상 100% 전제 | **30.7 ~ 42.6%** | — |
| 요청 단가 | — | **$0.001015** | — |

| 시나리오 | §10.2 추정 | 실측 환산 | 오차 |
|---|---|---|---|
| A — 프리베이크 2,880건 | $0.94 | **$5.12** | **+445%** |
| B — 재계획 20,000건/일 (월) | $195 | **$609** | +212% |
| C — 전면 재계획 120,000건/일 (월) | $1,170 | **$3,654** | +212% |

**무엇이 맞았고 무엇이 틀렸나.**

| 항목 | 판정 |
|---|---|
| 비용 **모델** (토큰 × 단가, 캐시 1/10 할인) | **맞았다.** 실측과 일치한다 |
| 요청당 토큰 | **틀렸다.** 축소판 카탈로그로 추정한 탓이고, 오차의 90%가 여기서 온다 |
| 캐시가 비용을 줄인다 | **맞았다.** 적중률 40.6%로 비용 33.4% 절감 |
| 캐시 적중률 95% 전제 | **틀렸다.** Gemini 암시적 캐싱은 요청 단위로 전부 적중/미적중이고 비결정적이다. 95%는 **명시적 캐싱**(Anthropic `cache_control`) 전제 값이다 |
| 절대 금액대 | **맞았다.** 5배 틀려도 월 $609는 단일 월드 운영비로 여전히 작다 |

> **B·C는 "실측 단가 × 계획의 요청 수"이지 실측이 아니다.** 런타임 재계획 요청 수는 아직 재지 않았다
> — 이 저장소의 회차는 전부 `--tier none`이라 런타임 LLM 호출이 0이다(`cost_actual.md §4`).
> 지금 실측인 것은 **요청 하나의 단가**뿐이다.

> **비용을 1/11로 줄이는 길이 따로 있다.** 2,880버킷 중 평시 하루에 실제로 도달하는 것은 **264개(9.2%)**다
> (`bucket_usage.md`). 미도달 버킷은 폴백이 덮으므로(시나리오 C가 확인) 다음 회차는 도달 집합만 만들면 된다.

### 10.3 정직한 재판정: §2.1의 "성립하지 않는다"는 **로컬 한정**이었다

외부 API를 쓰면 원안(5,000 NPC 전면 주기 재계획)은 **처리량도 비용도 성립한다.** 월 $288이면 R&D 예산에서 논쟁거리가 아니다. 이 부분은 v1의 판정을 수정한다.

그럼에도 캐시 아키텍처를 여전히 권장하는 이유는 셋이다. 비용이 아니다.

1. **가치 (§2.2)** — 플레이어가 동시에 인지하는 NPC는 20~50마리다. 5,000마리 전원 개별 계획은 대부분 관측되지 않는 연산이고, 그 사실은 예산이 남는다고 달라지지 않는다.
2. **지연 변동성** — 외부 API의 p99는 수십 초로 튄다. 게임 크리티컬 패스에 두면 안 된다는 원칙은 그대로다.
3. **가용성** — 외부 장애 시 NPC가 멈추면 안 된다. 캐시된 플랜이 곧 폴백이다.

그리고 캐시가 있으면 **같은 예산으로 훨씬 좋은 모델을 쓸 수 있다.** 2,880개 아키타입 플랜에 Claude Haiku 4.5를 써도 1회성 $3.31이다.

> **원칙: 재사용 횟수에 비례해 품질에 투자한다.**
> 수천 NPC가 공유하는 아키타입 플랜(2,880개) → 최고 품질 모델. 비용은 무시 가능하다.
> 1마리 1회용 임시 재계획 → 로컬 소형 모델로 충분하다.

### 10.4 3-티어 라우팅

| 티어 | 엔진 | 담당 | 지연 | 비용 |
|---|---|---|---|---|
| **T0** | 플랜 캐시 | 히트 (목표 98%+) | μs | 0 |
| **T1** | 로컬 dotLLM | 개별 이벤트 재계획, 야간 배치, 외부 장애 시 폴백 | ~1.75s | 전기료 |
| **T2** | 외부 API | 프리베이크, 대량 재계획, 고품질 아키타입 플랜, T1 큐 폭주 시 스필오버 | ~2s (p99는 수십 초) | §10.2 |

`IChatClient` 뒤에 라우터를 두면 호출부는 티어를 모른다.

```csharp
// Npc.Llm/TieredChatClient.cs (개념)
public sealed class TieredPlanClient
{
    // T1 큐 깊이가 임계를 넘거나, 요청이 HighQuality 플래그면 T2로
    IChatClient Route(PlanRequest r) =>
        r.Quality is PlanQuality.Archetype        ? _external :
        _localQueue.Depth > _spilloverThreshold   ? _external :
                                                    _local;
}
```

일일 토큰 예산에 **하드 캡**을 건다. 초과하면 T2 요청을 자동으로 T1으로 강등시키고 경보를 낸다. 비용 폭주는 외부 API 도입 시 가장 흔한 사고다.

### 10.5 외부 API 사용 시 추가 주의점

**(a) 프리픽스를 오히려 키워야 한다 — 반직관적이지만 중요하다**

프롬프트 캐시가 걸리는 **최소 토큰 임계**가 제공사마다 다르다.

| 제공사 | 최소 캐시 길이 | TTL |
|---|---|---|
| OpenAI | 1,024 토큰 | 비활성 5~10분 (최대 1시간), GPT-5.5+ 는 24시간 옵션 |
| Anthropic | 모델별 512 / 1,024 / **Haiku 4.5는 4,096** | 기본 5분, 1시간은 write 2배 |
| Gemini | 2.5 계열 2,048 / 3.x 계열 **4,096** | explicit 기본 1시간 |

로컬만 볼 때는 프리픽스를 줄이는 게 이득이지만, 3사 모두에서 캐시를 태우려면 **고정 프리픽스를 4,096 토큰 이상으로 채워야 한다.** 액션 카탈로그 설명을 넉넉히 쓰고 few-shot을 늘려서 의도적으로 채우는 편이 낫다. §4.2의 1,500 토큰 설계는 **4,200~4,500 토큰으로 상향**한다.

**(b) 대량 잡은 몰아서 돌린다 — §11의 시분할과 정면으로 충돌하는 지점이다**

캐시 TTL이 5분~1시간이므로 프리베이크를 띄엄띄엄 돌리면 cache write를 반복 지불한다. Anthropic은 write에 1.25~2배 할증까지 붙는다.

> **시분할은 T1(로컬)과 런타임 개별 재계획에 적용한다. T2(외부) 대량 잡은 반대로 몰아서 한 번에 돌린다.**

**(c) 스키마 강제 방식이 제공사마다 다르다**

GBNF는 로컬 전용이다. 이식 가능한 최소 공통분모는 JSON Schema뿐이고, `strict` 지원 수준도 서비스마다 다르다. → **§3.3의 4단 검증기 중요도가 더 올라간다.** 강제 디코딩을 신뢰하지 말고 항상 검증한다.

**(d) Rate limit** — RPM/TPM에 걸린다. 429에 지수 백오프 + AIMD로 동시성 자동 조절을 넣는다. 프리베이크에서 동시 32를 그냥 던지면 초반에 다 튕긴다.

**(e) 결정론** — `temperature=0`, seed 고정으로도 외부 API는 완전 재현이 안 된다. 다만 §R5대로 **플랜을 생성 시점에 저장**하므로 버그 재현에는 문제가 없다. 컴파일러 방식의 이점이 여기서 또 나온다.

---

## 11. 시분할 스케줄링 — 직관은 맞고, 이득의 출처가 다르다

"1,000개면 한 번에 200개씩" 아이디어에는 서로 다른 두 개념이 섞여 있다. 분리해서 보면 하나는 이득이 0이고 하나는 큰 이득이다.

- **(a) 요청 패킹** — 200 NPC 상태를 **1개 LLM 요청**에 담아 200개 플랜을 한 번에 받는다
- **(b) 시분할** — 200개씩 **나눠서 시간차로** 처리한다 (요청은 각각 별개)

### 11.1 요청 패킹의 이득은 로컬에서 정확히 0이다

| 패킹 크기 | 요청당 소요 | 플랜 1개당 |
|---|---|---|
| 1개 | 1.75s | **1.750s** |
| 8개 | 14.00s | **1.750s** |
| 50개 | 87.50s | **1.750s** |
| 200개 | 350.00s | **1.750s** |

완전히 동일하다. 이유는 둘이다.

1. 패킹의 유일한 이론적 이득은 **고정 프리픽스 비용의 분산**인데, prompt cache가 이미 그걸 무료로 만들었다.
2. decode 시간은 **출력 토큰 수에 선형**이다. 200개 플랜 = 30,000 출력 토큰이고, 이건 1개씩 200번 뽑든 한 번에 뽑든 같다.

외부 API에서는 요청당 네트워크 RTT(0.2~0.5s)가 분산되어 소폭 이득이 있지만, **동시 요청을 32로 올리는 쪽이 비교할 수 없이 큰 레버다.** 그리고 패킹의 손실은 확실하다.

- 출력이 길어질수록 스키마 이탈·품질 저하가 급격히 증가한다
- 응답 1개가 실패하면 **200개가 통째로 폐기**된다. 재시도 단위가 200배가 된다
- 부분 실패를 다룰 방법이 없다

> **결론: 패킹의 실용 상한은 4~8개다. 기본값은 패킹하지 않는 것으로 둔다.** 200개는 안 된다.

### 11.2 시분할의 진짜 이득은 총량이 아니라 **선택권**이다

먼저 분명히 할 것: **시분할은 총 처리량 요구를 줄이지 않는다.** 1,000개를 200개씩 5번에 나눠도 총 1,000 요청이다. 줄어드는 건 피크뿐이다.

피크 평활화 자체도 가치가 있다 — 큐 깊이가 유계가 되고 p99가 예측 가능해진다. 하지만 진짜 이득은 다른 데 있다.

> **슬라이스로 나누는 순간 "누구를 재계획할지 고를 수 있게 된다."** 라운드로빈으로 200개를 순서대로 도는 게 아니라, **"지금 재계획이 가장 필요한 200개"** 를 우선순위로 뽑으면 총 요청 수 자체가 줄어든다.

```
우선순위 점수 =  w1 · 플레이어 근접도       (관측될 확률)
              +  w2 · 플랜 노후도            (마지막 재계획 이후 경과)
              +  w3 · 전제조건 이탈도        (현 플랜의 전제가 얼마나 깨졌나)
              +  w4 · 이벤트 긴급도          (전투/재해/플레이어 개입)
```

이 큐에서 **매 슬라이스마다 예산만큼만 상위 K개**를 꺼내 쓴다. 예산이 부족하면 자연히 중요한 NPC만 갱신되고, 나머지는 기존 플랜을 계속 돌린다. 우아하게 열화(graceful degradation)되는 구조다. `w1`을 크게 잡으면 §2.2에서 지적한 "관측되지 않는 연산" 문제도 같이 해결된다.

### 11.3 상태 체크의 시분할 — 여기가 제일 큰 이득이다 (LLM과 무관)

사실 원 아이디어에서 "상태 체크"를 나눈다는 부분이 더 중요하다. 5,000 NPC의 *"재계획이 필요한가"* 판정을 매 틱 전수로 돌리면 그것 자체가 CPU를 먹는다. 이건 LLM 비용과 완전히 별개의 순수 CPU 문제다.

인지 LOD 등급별로 스캔 주기를 달리한다.

| 등급 | 대상 | 마리수 | 스캔 주기 | 틱당 판정 |
|---|---|---|---|---|
| L0 | 플레이어 시야 내 | 50 | 매 틱 | 50.0 |
| L1 | 동일 존 | 450 | 10틱 | 45.0 |
| L2 | 원거리 | 2,000 | 100틱 | 20.0 |
| L3 | 비활성 | 2,500 | 이벤트 시에만 | 0.0 |
| | | **5,000** | | **115.0** |

전수 스캔 5,000/틱 대비 **43배 감소**한다. 판정 자체도 싸게 만든다 — 플랜 전제조건을 비트마스크로 인코딩해두고 월드 상태 비트마스크와 XOR 한 번으로 이탈 여부를 본다.

```csharp
// Npc.Runtime/CognitionScheduler.cs (개념)
// SoA 레이아웃 + 슬라이스 인덱스로 캐시 지역성 유지
readonly struct Slice { public readonly int Start, Count; }

void Tick(long tick)
{
    foreach (var lod in _lods)                       // L0..L2
    {
        if (tick % lod.Period != 0) continue;
        var s = lod.Slices[(tick / lod.Period) % lod.Slices.Length];
        for (int i = s.Start; i < s.Start + s.Count; i++)
            if ((_planPreconditions[i] & ~_worldState[i]) != 0)  // 전제 이탈
                _replanQueue.TryEnqueue(i, Score(i));
    }
}
```

### 11.4 2-경로 설계: 느린 배경 스캔 + 즉시 인터럽트

슬라이스 스캔만 두면 L2 NPC는 최악 100틱 뒤에야 전투를 인지한다. 그건 안 된다.

- **배경 경로** — 슬라이스 스캔. 느리고, 주기적이고, 전체를 훑는다. "서서히 어긋난 것"을 잡는다
- **인터럽트 경로** — 이벤트 발생 시 슬라이스를 기다리지 않고 **즉시** 우선순위 큐에 최고 점수로 삽입한다. 전투·플레이어 개입·재해가 여기 해당한다

그리고 §3.2대로 **인터럽트에 대한 즉각 반응 자체는 LLM이 아니라 런타임 규칙이 처리한다.** LLM 재계획은 그 뒤에 비동기로 따라온다. NPC는 먼저 도망치고, 나중에 새 계획을 받는다.

---

## 12. v2 반영 — 로드맵 변경분

| 주차 | 변경 내용 |
|---|---|
| **W1** | 스파이크에 **외부 API 대조군 추가.** 로컬 dotLLM / Ollama / 외부 API(GPT-5 nano, Qwen3.5 Flash, Claude Haiku 4.5) 4종을 같은 프롬프트로 비교. **프리픽스를 4,200~4,500 토큰으로 설계**하고 3사 캐시 적중 실측 |
| **W2–4** | `CognitionScheduler`(§11.3) + 인지 LOD 슬라이싱을 런타임 1차 구현에 포함. 이 단계는 LLM 없이 순수 CPU 부하만 측정 |
| **W5–6** | 변경 없음. 단 검증기 중요도 상향 (§10.5c) |
| **W7–8** | 프리베이크를 **외부 API 동시 32로 실행** (84분 → 3분). AIMD 동시성 조절 + 429 백오프 포함 |
| **W9–10** | **3-티어 라우터 + 스필오버 + 일일 토큰 하드 캡** 구현 (§10.4). 우선순위 큐(§11.2) 가중치 튜닝. 5,000 NPC 부하 테스트 |
| **W11–12** | 변경 없음. 단 **T2 장애 주입 테스트** 추가 — 외부 API를 끊었을 때 게임이 정상 동작하는지 |

### 추가된 성공 기준

- 요청 패킹 크기는 **1(기본)** 이며, 8까지 올렸을 때 품질 저하가 없는지 측정한다
- 인지 스캔 부하: 틱당 판정 대상 **≤ 150마리** (5,000 전수 대비 30배 이상 감소)
- 외부 API 프롬프트 캐시 적중률 **≥ 95%** (프리베이크 잡 기준)
- **T2 전면 차단 상태에서 5,000 NPC가 정상 동작**한다 (캐시 + T1 + 폴백만으로)
- 일일 토큰 예산 초과 시 자동 강등이 동작한다

### 리스크 대장 추가분

| # | 리스크 | 대응 |
|---|---|---|
| R8 | **비용 폭주** — 버그로 재계획 루프가 돌면 하루에 예산을 태운다 | 일일 토큰 하드 캡 + T1 자동 강등 + 요청 수 이상 탐지 경보 |
| R9 | **외부 API 의존** — 장애·가격 인상·모델 폐기(deprecation) | T1 폴백 상시 유지. W11–12 장애 주입 테스트를 게이트로 |
| R10 | **패킹 유혹** — 처리량 개선으로 착각해서 패킹 크기를 키움 | §11.1을 근거로 기본값 1 고정. 변경 시 품질 회귀 테스트 필수 |
| R11 | **캐시 미적중** — 프리픽스에 동적 값이 섞여 캐시가 전부 깨짐 | 프리픽스 해시를 로그에 남기고, 유니크 해시가 1개인지 상시 검증 |

---
---

# v3 개정 — 결과물 · 게임서버 연동 · 마스터데이터

## 13. 12주 후 손에 남는 것

전체 명세는 [`docs/00_Deliverables.md`](docs/00_Deliverables.md). 여기서는 한 문장과 표만 남긴다.

> **5,000마리 NPC가 마스터데이터로 정의된 가상 마을에서, LLM이 생성한 행동 플랜에 따라 살아 움직이고, 그 결과를 게임서버용 명령 패킷으로 쏟아내는 헤드리스 서버. 그리고 그 패킷을 받아 월드를 갱신하고 되돌려주는 게임서버 대역.**

| 구분 | 산출물 |
|---|---|
| **실행 바이너리** | `Npc.Host` (NPC 서버) · `Npc.SimHost` (게임서버 대역) · `Npc.Prebake` (플랜 생성 CLI) · `Npc.Replay` |
| **데이터 아티팩트** | 마스터데이터 11종 · **프리베이크 플랜 2,880개** · 골든 픽스처 50건 · 리플레이 로그 |
| **코드 자산** | `Npc.Contracts`(게임서버 IF) 외 9개 프로젝트 |
| **관측/보고** | 운영 대시보드(HTML) · R&D 결과 보고서 |

**실질적 핵심 산출물은 코드가 아니라 `planstore/`의 플랜 2,880개다.** 사람이 읽고 고칠 수 있는 JSON이고, 이게 곧 오써링 자동화의 증거물이다.

### 데모 3종

| 시나리오 | 내용 | 검증 대상 |
|---|---|---|
| **A. 살아있는 마을** | 게임 7일 × 60배속, NPC 5,000 | 기본 동작 · 성능 · 캐시 히트율 |
| **B. 공성 이벤트** | 지역 상태를 War로 전환 → 마을 전체 행동 변화 | 재계획 · 인터럽트 · 아키타입별 차별화 |
| **C. LLM 전면 차단** | T2 → T1 → 캐시 순차 차단 | 가용성 · 폴백 |

---

## 14. 게임서버 연동 — 인터페이스까지만

전체 사양은 [`docs/02_GameServer_Link.md`](docs/02_GameServer_Link.md).

**이번 범위: `IGameServerLink` 인터페이스와 패킷 정의까지.** 소켓·직렬화·재접속은 만들지 않는다.
**목표: 나중에 실제 게임서버에 붙일 때 NPC 서버 코드가 한 줄도 안 바뀌는 것.**

```csharp
public interface IGameServerLink : IAsyncDisposable
{
    void      Enqueue(in NpcCommand command);        // NPC서버 → 게임서버 (fire-and-forget)
    ValueTask FlushAsync(CancellationToken ct);      // 배치 송출
    ChannelReader<GameEvent> Events { get; }         // 게임서버 → NPC서버
    LinkState State { get; }
    LinkStats Stats { get; }
}
```

### 인터페이스만 만들고 나중에 네트워크를 붙이는 계획이 실패하는 이유

거의 항상 같다 — **인프로세스라서 공짜였던 가정이 네트워크에서 깨진다.** 그래서 8개 규칙을 컴파일 타임 또는 테스트로 강제한다.

| # | 규칙 | 강제 방법 |
|---|---|---|
| N1 | 명령은 fire-and-forget. 결과는 이벤트로만. **RPC 왕복 금지** | 반환값 있는 메서드를 두지 않음 |
| N2 | 모든 패킷은 순수 값 타입 (`readonly record struct`) | 분석기 + 테스트 |
| N3 | **문자열 금지, 전부 ID** | 리플렉션 테스트로 `string` 필드 0개 확인 |
| N4 | 시간은 `Tick`(long)만. `DateTime` 금지 | 동일 |
| N5 | 모든 명령에 `CorrelationId` | 헤더 필수 |
| N6 | 이벤트에 `SequenceNumber` (유실·중복 검출) | 링크가 갭 경보 |
| N7 | 이벤트 처리는 멱등 | 동일 이벤트 2회 주입 → 상태 해시 동일 |
| N8 | 배치가 기본. 단건은 배치 크기 1 | `SendBatch`만 노출 |

**N1이 가장 중요하다.** 네트워크 RTT가 틱 루프를 블록하면 그 순간 끝난다.

### 명령 / 이벤트 개요

| 방향 | 종류 | 예 |
|---|---|---|
| NPC서버 → 게임서버 | 12종 | `Spawn`, `MoveTo`, `Interact`, `Speak(DialogueId)`, `CombatAction`, `InventoryChange` … |
| 게임서버 → NPC서버 | 17종 | `TickSync`, `NpcArrived`, `NpcActionFailed`, `PlayerProximity`, `ZoneStateChanged`, `DamageTaken` … |

**명령 유실 방어**: 각 스텝에 `timeout_s`가 있고, 응답 이벤트가 안 오면 로컬에서 `ActionFailed(Timeout)`을 합성한다. 네트워크에서 명령은 반드시 유실되므로 인프로세스 단계부터 이 경로를 돌려둔다 (`Npc.Sim`의 `--drop-rate`).

### 구현체

| 구현체 | 이번 범위 |
|---|---|
| `LoopbackGameServerLink` (Sim 직결) | ✅ 구현 — W2~W10 주력 |
| `NullGameServerLink` | ✅ 구현 |
| `RecordingGameServerLink` / `ReplayGameServerLink` | ✅ 구현 — 결정론 검증 |
| `TcpGameServerLink` | ❌ 골격 + TODO만. **컴파일은 되어야 한다** |

---

## 15. 마스터데이터

전체 스키마는 [`docs/01_MasterData_Spec.md`](docs/01_MasterData_Spec.md).

> **원칙: 마스터데이터가 LLM 프롬프트의 단일 원천이다.** 액션 카탈로그에서 프롬프트 프리픽스를 *생성*한다. 둘이 어긋나면 검증 실패율이 즉시 치솟는다.

| 파일 | 규모 | 역할 |
|---|---|---|
| `world_flags.json` | 42개 (64bit, 22비트 예약) | 전제조건·효과·이탈 판정의 기반. **가장 먼저 확정** |
| `actions.json` | **37개** | LLM이 조합할 유일한 원자 단위. 프롬프트 본문 |
| `items.json` | ~80 | 아이템 + 레시피. `grants`로 플래그 연결 |
| `zones.json` / `pois.json` | 12 / ~250 | 공간. + 거리 행렬 사전계산 |
| `archetypes.json` | **40종** | 허용 액션·성향·기본 목표·인구 비중 |
| `context_buckets.json` | 6×4×3 | 시간대·지역상태·기후 → **버킷 2,880** |
| `interrupts.json` | ~12 규칙 | 즉시 반응. **LLM이 만들지 않는다** |
| `fallback_plans.json` | 40개 | 최후 보루. **사람이 직접 작성** |
| `npc_instances.json` | 5,000 | 스크립트 생성 |
| `prompt/` | 4,200~4,500 tok | 시스템 규칙 + 카탈로그 + few-shot |

### 세 가지 핵심 설계

**1. WorldFlags 64비트가 전부를 관통한다**

```csharp
// 인지 스캔의 핫패스 — 틱당 115회. 비트 연산 한 번으로 끝난다.
static bool NeedsReplan(WorldFlags cur, CompiledPlan p)
    => (p.RequiredFlags & ~cur) != 0 || (p.ForbiddenFlags & cur) != 0;
```

플랜 단위로 `RequiredFlags`를 사전 OR 해두기 때문에 스텝을 순회할 필요가 없다. 이게 틱 예산 20ms를 지키는 근거다.

**2. 버킷 인덱스는 배열 첨자다**

```csharp
public int ToIndex() => ((A.Value * 6 + (int)T) * 4 + (int)R) * 3 + (int)C;   // 0..2879
```

플랜 스토어는 `CompiledPlan?[2880]` 고정 배열. 해시맵도 락도 필요 없다.

**3. 폴백 플랜 40개 수작성이 오써링 자동화의 비교 기준선이다**

이 프로젝트에서 사람 손이 가장 많이 가는 부분이고(약 10시간 예상), 동시에 W12 보고서의 분모다. **작성 시간을 반드시 기록한다.**

### 작업 순서

의존 순서대로 만들어야 한다. 역순이면 계속 되돌아온다.

```
world_flags → items → actions → zones → pois → archetypes
→ context_buckets → interrupts → fallback_plans(40개 수작성) → prompt(자동생성) → npc_instances(스크립트)
```

### 기동 시 검증 V1~V11

전부 **기동 실패 조건**이다. 경고 후 진행을 허용하면 반드시 런타임에 터진다. 특히:

- `V6` 버킷 조합 수 == 2,880
- `V7` 모든 아키타입에 폴백 플랜이 있고 검증 4단을 통과
- `V9` 프롬프트 프리픽스 ≥ 4,096 토큰 (외부 API 캐시 임계)
- `V11` 고립 POI 없음

---

## 16. 문서 맵

| 문서 | 내용 |
|---|---|
| **`LLM_NPC_Server_Plan.md`** | (이 문서) 상위 계획 · 타당성 판단 · 아키텍처 · 리스크 |
| [`docs/00_Deliverables.md`](docs/00_Deliverables.md) | 결과물 명세 · 데모 시나리오 · 최종 수용 기준 |
| [`docs/01_MasterData_Spec.md`](docs/01_MasterData_Spec.md) | 마스터데이터 11종 전체 스키마 · 검증 규칙 · 작업 순서 |
| [`docs/02_GameServer_Link.md`](docs/02_GameServer_Link.md) | 게임서버 IF · 패킷 29종 · 네트워크 안전 규칙 N1~N8 · Sim 사양 |
| [`docs/03_PlanDSL_Spec.md`](docs/03_PlanDSL_Spec.md) | 플랜 DSL · JSON Schema · 4단 검증기 · 실행기 · 스토어 포맷 |
| [`docs/10_Phase0_W1_Spike.md`](docs/10_Phase0_W1_Spike.md) | **W1** 스파이크 — 숫자 6개를 뽑는다 |
| [`docs/11_Phase1_W2-4_Core_Runtime.md`](docs/11_Phase1_W2-4_Core_Runtime.md) | **W2–4** 코어 · 런타임 · Sim — LLM 없이 돌린다 |
| [`docs/12_Phase2_W5-6_Plan_Compiler.md`](docs/12_Phase2_W5-6_Plan_Compiler.md) | **W5–6** 플랜 컴파일러 · 검증기 · 통과율 개선 |
| [`docs/13_Phase3_W7-8_Plan_Cache.md`](docs/13_Phase3_W7-8_Plan_Cache.md) | **W7–8** 플랜 캐시 · 프리베이크 · 검수 |
| [`docs/14_Phase4_W9-10_Scheduler_Tiering.md`](docs/14_Phase4_W9-10_Scheduler_Tiering.md) | **W9–10** 우선순위 큐 · 3-티어 라우팅 · 부하 테스트 |
| [`docs/15_Phase5_W11-12_Verification.md`](docs/15_Phase5_W11-12_Verification.md) | **W11–12** 골든 · 결정론 · 장애주입 · 블라인드 평가 · 보고 |

### 읽는 순서 (구현 착수 시)

```
1. 이 문서 §1~§12          왜 이 구조인가
2. docs/00                 무엇을 만드는가
3. docs/01 → 02 → 03       공통 계약 3종. 여기서 정한 타입 이름을 전 코드가 쓴다
4. docs/10                 W1 착수
5. 이후 주차별로 docs/11~15
```

---

## 17. 프로젝트가 실패해도 남는 것

정직하게 적어둔다. **최악의 경우에도 버려지는 것은 LLM 부분뿐이다.**

| 실패 양상 | 남는 자산 |
|---|---|
| 블라인드 평가에서 LLM 플랜이 열등 | 마스터데이터 스키마 · 결정론 NPC 런타임 · 인지 LOD 스케줄러 · 게임서버 IF · Sim → **LLM 없는 NPC 서버로 그대로 쓸 수 있다** |
| 검증 통과율이 끝내 90% 미달 | 4단 검증기. 사람이 짠 플랜의 정합성 검사에 그대로 쓴다 |
| 비용이 예상보다 10배 | 프리베이크 전용으로 축소. 런타임 LLM만 포기 |
| dotLLM이 불안정 | `IChatClient` 추상화 덕에 엔진 교체만 |

**이게 이 프로젝트를 시작해도 되는 이유다.** 최악의 경우에도 결정론적 NPC 행동 시스템 하나가 남고, 그건 어차피 만들어야 했던 것이다.

---

## 참고

- [dotLLM — Native .NET LLM Inference](https://dotllm.dev/)
- [kkokosa/dotLLM (GitHub)](https://github.com/kkokosa/dotLLM)
- [dotLLM ROADMAP.md](https://github.com/kkokosa/dotLLM/blob/main/docs/ROADMAP.md)
- [Introducing dotLLM - Building an LLM Inference Engine in C#](https://kokosa.dev/blog/2026/dotllm/)
- [Open-Weight LLM Models to Run Locally in 2026](https://huggingface.co/blog/daya-shankar/open-source-llm-models-to-run-locally)
- [LLM API Pricing Comparison July 2026 — BenchLM](https://benchlm.ai/llm-pricing)
- [Prompt Caching in 2026: OpenAI vs Claude vs Gemini Pricing](https://leanlm.ai/blog/prompt-caching)
