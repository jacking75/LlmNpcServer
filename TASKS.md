# TASKS — 작업 지시서 인덱스 · 진행 원장

> `docs/1x_*_Spec/Phase` 문서가 **"무엇을 왜 이렇게 만드는가"(사양)** 라면,
> `docs/1x_*_TASKS.md`는 **"지금 무엇을 어떤 순서로 어디에 만들고 무엇이 되면 끝인가"(작업 지시)** 다.
>
> **이 문서와 TASKS 문서에는 사양을 복사하지 않는다.** 내용은 `docs/`를 참조만 한다. 복사하는 순간 어긋나기 시작한다.

---

## 1. 태스크 규약

### 1.1 형식

```
**T2-014** 태스크 이름 · `M` · 선행 T2-011, T2-012
  파일  src/Npc.Core/Catalog/ActionCatalog.cs (신규)
  사양  docs/01 §2 · docs/03 §5
  내용  한두 문장. 무엇을 만드는지. 사양에 있는 내용은 반복하지 않는다.
  완료  ActionCatalog_IndexesByCode 통과 · 37개 전부 로드 · 미정의 flag 참조 시 예외
```

| 필드 | 규칙 |
|---|---|
| **ID** | `T{phase}-{nn}`. phase는 0~5. 한 번 부여하면 재사용하지 않는다 |
| **규모** | `S` ≤1시간 · `M` 2~4시간 · `L` 1일. **L은 쪼갤 수 없을 때만** |
| **선행** | 이 태스크가 시작 가능하려면 완료돼 있어야 하는 태스크 |
| **파일** | 생성/수정할 경로. `(신규)`/`(수정)` 명시 |
| **사양** | 근거 문서와 절 번호. **에이전트는 이걸 먼저 읽는다** |
| **완료** | 통과해야 할 테스트 이름 또는 관측 가능한 조건. 주관적 표현 금지 |

### 1.2 크기 원칙

- **1 태스크 = 1 커밋.** 커밋 메시지는 `T2-014: ActionCatalog 인덱스 구축`
- 파일 1~3개, 테스트 1~2개 이내. 넘으면 쪼갠다
- 에이전트 세션 1회에 끝나야 한다. 중간에 컨텍스트가 끊기면 태스크가 너무 큰 것이다
- **완료 조건에 테스트가 없는 태스크는 만들지 않는다.** 예외: 순수 데이터 작성(마스터데이터 JSON), 문서 작성

### 1.3 상태 표기

원장(§3)에서만 관리한다. 태스크 본문은 고치지 않는다.

| 표기 | 뜻 |
|---|---|
| ` ` | 미착수 |
| `~` | 진행 중 |
| `x` | 완료 (테스트 통과 + 커밋됨) |
| `!` | 막힘 — 원장 비고에 이유 기록 |
| `-` | 취소/불필요 — 이유 기록 |

---

## 2. 코딩 에이전트 작업 루프

에이전트에게 주는 지시는 항상 이 형태다.

```
CLAUDE.md 를 읽고, docs/11_Phase1_W2-4_TASKS.md 의 T1-028 을 구현해라.
사양 필드에 적힌 문서를 먼저 읽고 시작해라.
완료 조건의 테스트를 같이 작성해라.
```

에이전트가 매번 따라야 할 순서:

```
1. CLAUDE.md 의 §2 절대 규칙을 확인한다
2. 태스크의 "사양" 필드 문서를 읽는다      ← 건너뛰면 반드시 타입 이름이 어긋난다
3. 선행 태스크가 원장에서 x 인지 확인한다
4. "파일" 에 적힌 경로에만 만든다
5. "완료" 의 테스트를 작성하고 통과시킨다
6. dotnet build && dotnet test 전체 통과 확인
7. 원장의 해당 태스크를 x 로 바꾸고, 커밋한다
```

### 한 번에 여러 태스크를 주지 않는다

선행 관계가 없는 태스크라도 한 세션에서 3개 이상 처리하면 리뷰가 불가능해진다. **연속으로 시킬 때도 태스크마다 커밋을 끊는다.**

### 사양과 어긋나면 멈춘다

태스크를 구현하다 `docs/`의 사양이 틀렸거나 불충분하다고 판단되면, **임의로 코드에 맞추지 말고 멈추고 보고한다.** 사양을 먼저 고치고(같은 커밋에서) 진행한다.

---

## 3. 진행 원장

### 전체

| Phase | 문서 | 태스크 | 완료 | 상태 |
|---|---|---|---|---|
| **P0** W1 스파이크 | [`docs/10_Phase0_W1_TASKS.md`](docs/10_Phase0_W1_TASKS.md) | 13 | 8 | 진행 중 — **게이트 1/4 통과** (T0-09~T0-12 `~`, T0-13 미착수) |
| **P1** W2–4 코어·런타임·Sim | [`docs/11_Phase1_W2-4_TASKS.md`](docs/11_Phase1_W2-4_TASKS.md) | 62 | 62 | **게이트 통과** |
| **P2** W5–6 플랜 컴파일러 | [`docs/12_Phase2_W5-6_TASKS.md`](docs/12_Phase2_W5-6_TASKS.md) | 24 | 23 | 태스크 완료 (T2-23 은 조건 미충족 `-`) — **게이트 3/7 미달** |
| **P3** W7–8 플랜 캐시 | [`docs/13_Phase3_W7-8_TASKS.md`](docs/13_Phase3_W7-8_TASKS.md) | 21 | 19 | 진행 중 — T3-18(사람 검수) 미착수 · T3-21 `~`. **게이트 5/9** (기제 4항목 통과, 실측 산출물 대기) |
| **P4** W9–10 스케줄러·티어링 | [`docs/14_Phase4_W9-10_TASKS.md`](docs/14_Phase4_W9-10_TASKS.md) | 24 | 0 | 미착수 (W1 실측 반영 완료). **T4-07 의 라우팅 부분만 T5-08 이 먼저 만들었다** — 예산·스필오버·서킷브레이커는 그대로 남아 있다 |
| **P5** W11–12 검증·평가 | [`docs/15_Phase5_W11-12_TASKS.md`](docs/15_Phase5_W11-12_TASKS.md) | 20 | 16 | 진행 중 — **W11 완료** · W12 진행 |
| | | **164** | **128** | |

각 Phase 문서 맨 아래의 체크리스트에서 개별 태스크 상태를 관리한다.

> **P0 이 P1 보다 뒤에 있다.** §4 는 "게이트 미통과 시 다음 Phase 로 넘어가지 않는다"고 했지만 P1 이 먼저 끝났다.
> P1 은 LLM 을 한 번도 부르지 않는 범위라(`P1_gate.md` #2) 이 역전의 피해는 없었다. **영향은 전부 P2 이후에 있다.**

### 막힌 태스크 / 결정 대기

| ID | 내용 | 막힌 이유 | 필요한 결정 |
|---|---|---|---|
| T0-09 | 게이트 G0-1 (전 엔진 유효 JSON 100/100) | 100/100 은 `llamacpp-qwen3-8b` 하나. 나머지 94~99/100 | **강제 디코딩을 기본에서 뺀다**로 처리했다(`docs/10` 게이트 절). 그래도 G0-1 기준 자체를 "유효 JSON"에서 "**어휘 검증 통과율**"로 바꿀지 결정 필요 — forced 는 유효 JSON 99%에 검증 0% 였다 |
| T0-10 | 게이트 G0-2 (지연 1.75s ±50%) | 실측 4B 3.7s · 8B 5.1s. 가정의 2~3배 | 상위 계획 §2.1/§10 과 `CLAUDE.md §9` 를 실측으로 갱신 (T0-13). `docs/14 §3` 예산표는 이미 고쳤다 |
| T0-10 | `W1_perf.csv` 가 14조합이 아니라 6조합 | 외부 API 3종을 성능 매트릭스에서 뺐다. 로컬 2종만 있다 | 외부 지연·비용을 T2-19 첫 회차로 미룰지, W1 을 연장해 지금 잴지 |
| T0-11 | 외부 API 동시성 미측정 | `W1_concurrency.md` 는 `llamacpp` 2종뿐. 429 가 한 번도 안 났다 | "동시 32"의 근거가 없다. T3-08 `--concurrency` 기본값을 8 로 두고 T2-19에서 실측하기로 했다 — 확인 필요 |
| T0-12 | 게이트 G0-4 (4B ≥ 8B의 80%) | 4B(7.54/10)만 채점. 8B 미채점이라 비교 대상이 없다 | **로컬 모델 확정을 못 한다.** 8B 를 같은 조건으로 채점할지, 4B 로 확정하고 넘어갈지 |
| T0-13 | `W1_results.md` 미작성 | M1~M6 표와 5개 결정이 비어 있다 | **P2 착수 전에 끝낸다.** 이게 남아 있어 P2~P5 문서가 M 번호 대신 원본 측정 파일을 직접 인용하고 있다 |
| T2-19 | 2,880 버킷 전량 생성을 돌릴 엔진 | **Gemini 무료 등급은 모델당 하루 500건**이다 (2026-07-26 T2-09 실측, `W6_compile_stats.md §2.2`). 3.1-flash-lite · 3.5-flash-lite · 3.5-flash 셋 다 이날 429 로 막혔다 | 넷 중 하나를 골라야 한다: (1) 유료 키로 전환 (2,880건 ≈ $1 규모) (2) 무료 등급을 모델·날짜로 쪼개 2~3일 (3) 로컬 엔진(LM Studio/dotLLM) — 무료지만 W1 실측 3.7~5.1s/건이라 3~4시간 (4) 이미 환경에 있는 OpenRouter/POE 키 사용 — 사용자 크레딧이 소모된다 |
| **P2 게이트** | 7개 중 3개 통과 — **미달**. 통과율 78.3%(고른 표본 60) / 64.6%(연속 288, 제작 계열) · 폴백 15.6% · 캐시 41.7% · 다양성 37.1% | 통과율의 구조적 원인은 개선 루프에서 소진했다. 남은 실패는 28개 아키타입에 흩어진 긴 꼬리라 단일 처방이 없다 (`W6_compile_stats.md §8`) | 셋 중 하나: (1) 상위 모델 — 같은 표본에서 75~85%, 비용 2배 (2) 재시도 2회 — `docs/12 §6` 이 금지 (3) **게이트 기준 재검토** — 폴백·인접 재사용이 "절대 null 아님"을 이미 보장하므로 90% 는 품질 목표이지 동작 요건이 아니다 |
| **W12 경보** | 플랜 다양성 **37.1%** (아키타입 안 51.6%) — 목표 60% 미달 | `docs/12 §10` 이 예고한 대로다. 흩어진 표본으로 재면 75% 가 나오지만 그것은 "아키타입이 다르면 플랜도 다르다"를 재는 것이라 무의미하다. 연속 288버킷에서 같은 아키타입의 72개 상황을 보면 51.6% 다 | W12 블라인드 평가에서 **"구분 못 함"이 나올 가능성이 높다.** 지표가 "피습 시 대피" 같은 옳은 수렴을 벌하는 면도 있어, W12 전에 정의를 다듬을지(인자까지 포함 등) 결정 필요 |
| T2-19 | 전량 2,880 회차 | 연속 288버킷(제작 계열 4종)까지만 돌렸다. 소요 105초 · $0.46 | 전량은 약 27분 · $6 이고 통과율 판정을 바꾸지 못한다. 모델·게이트 기준을 정한 뒤 P3 프리베이크(T3-08)와 같이 돌리는 편이 낫다 |
| T2-23 | `count` 심볼화 | **실행 조건 미충족 — 취소(`-`)**. 조건은 "`V3.RESOURCE_IMBALANCE` 가 상위 원인이면" 인데, T2-21 마지막 회차에서 최종 실패 13건 중 1건(5위)이었다 (`W6_failures.md §2`). 레시피 입출력을 프리픽스에 실은 것(T2-03)으로 이미 잡혔다 | 다시 상위로 오면 그때 실행한다. `CompiledStep.Count` 예약값 인코딩은 `docs/03 §5` 에 아직 없다 |
| T3-18 | 검수 40건 기록 (`review_W8.jsonl`) | **사람이 하는 작업이다** (`docs/13 §5`). 도구(`tools/review.ps1`)·승격 도구(`tools/pin_plan.ps1`)는 완성됐고 표본 추출은 시드 고정이라 재현된다 | 검수 시점 결정. 40건에 사람 시간 약 40~80분. 여기서 나오는 채택률과 검수 시간이 W12 보고서의 핵심 수치다 |
| **P3 게이트** | 9개 중 5개 통과 — 기제 항목(5·6·7·8)과 폴백 해소는 통과. **나머지는 전량 회차·사람 검수 대기** (`P3_gate.md`) | 파일럿 288버킷 실측(115초 · $0.512 · 통과율 67.7% · 캐시 42.6% · 429 0회)의 전량 외삽이 **wall-clock 12~19분 · $5.12** 라 항목 2·3 은 그대로 미달이다. 항목 4(캐시 95%)는 명시적 캐싱 전제라 이 엔진 계열로는 도달 불가 | **2026-07-26 결정: 전량 회차를 미룬다.** T2-19 항목이 이미 적어 둔 판단과 같다 — 전량은 통과율 판정을 바꾸지 못하고, 항목 1의 근본 원인(통과율 67.7%)은 P2 미달이 그대로 이어지는 것이다. **P2 게이트 결정(상위 모델 / 재시도 2회 / 기준 재검토)이 정해진 뒤 그 모델로 한 번에 돌린다.** 그때 항목 2·4 의 기준도 같이 갱신한다 |
| T2-09 | 두 디코딩 모드의 1·2단 통과율 비교 | 위와 같은 이유로 forced 모드 20건을 못 돌렸다. prompt 모드는 유효 JSON 20/20 | T2-19 첫 회차에서 같이 가른다 (`docs/12` T2-19 가 이미 그렇게 적혀 있다) |
| — | W1 산출물 4건이 미커밋 | `spike/*.cs` 수정분과 `docs/measurements/W1_*` 가 워킹 트리에만 있다 | 규약상 `x` 는 커밋까지다. 커밋 시점 결정 |

### 사양 변경 이력

태스크 진행 중 `docs/`를 고쳤다면 여기 남긴다. W1 실측 반영이 대표적이다.

| 날짜 | 문서 | 변경 | 계기 |
|---|---|---|---|
| 2026-07-26 | `CLAUDE.md §3` · `docs/11 §2` | `Npc.Runtime` 의존에 `Npc.Planning` 추가 | T1-02. `docs/11 §4`의 `CognitionScheduler.Scan(..., PlanStore, ReplanQueue)` 시그니처가 이 간선을 요구한다. `Npc.Runtime ↛ Npc.Llm` 금지는 그대로 유지 |
| 2026-07-26 | `docs/01 §2.1·§2.2` · `docs/03 §3` | 액션에 `requires_any`(OR 전제) 추가 | T1-12. §2.2 표의 `AtHome/AtTavern`·`HasCoin \| HasProduct`를 §2.1의 `requires`(AND)로는 표현할 수 없었다 |
| 2026-07-26 | `docs/01 §2.1` | `emits[].priority` 와 `emits[].map` 값 문법 명문화 | T1-12. 역압 시 드롭 순서(`docs/02 §1`)를 데이터가 정해야 하고, `Eat`의 소비 아이템을 코드에 하드코딩하지 않으려면 `$first:Flag` 심볼이 필요했다 |
| 2026-07-26 | `docs/01 §2.2` | `Sleep`의 grants 를 `IsRested (clears IsSleeping)` 로 정정 | T1-12. `grants`에 `IsSleeping`이 남으면 `loop: true` 플랜의 첫 스텝(`MoveTo`, forbids `IsSleeping`)이 항상 V3.FORBIDDEN_FLAG 로 걸린다 |
| 2026-07-26 | `docs/02 §3.2` | `MoveTo`의 `TargetNpc`·`Zone` 용법 명문화 | T1-12. `Follow`·`Wander`가 발행할 명령이 없었다 |
| 2026-07-26 | `docs/01 §6` · `docs/11` T1-17 | `BucketKey` 를 `Npc.Planning` → `Npc.Core/Planning` 으로 이동 | T1-17. `docs/03 §5`의 `CompiledPlan.Bucket`(Npc.Core)이 `BucketKey`를 들고 있어 `Core → Planning → Core` 순환이 생겼다. `TimeOfDay`는 `GameClock`(Runtime)도 쓴다 |
| 2026-07-26 | `docs/03 §5·§8` | `CompiledStep` 의 `WorldFlags` 4종을 `CompiledPlan.StepFlagSets` 병렬 배열로 분리 | T1-23. 플래그만 40바이트라 §8의 `Unsafe.SizeOf<CompiledStep>() <= 32` 와 양립할 수 없었다. 실측 14바이트 |
| 2026-07-26 | `docs/03 §5` | `RequiredFlags` 를 '전 스텝 requires 의 OR' → '앞선 스텝이 세워주는 것을 뺀 진입 조건' 으로 정정 | T1-23. 단순 OR 이면 `Mine` 이 세워줄 `HasRawMaterial` 을 `Craft` 때문에 요구하게 되어 인지 스캔이 매 틱 이탈이라고 답한다 |
| 2026-07-26 | `docs/03 §5` | `IPlanVocabulary` 도입 (Npc.Core 선언 · Npc.MasterData 구현) | T1-23. `CompiledPlan`(Core)이 `ActionCatalog`(MasterData)를 필요로 하는데 CLAUDE.md §3 이 `Core → MasterData` 를 금지한다 |
| 2026-07-26 | `docs/01 §2.2` | `Wander` 파라미터에서 `zone`·`radius` 제거 (`duration_s` 만) | T1-23. 플랜에 존 id 가 박히면 (아키타입 × 버킷) 재사용이 깨진다 — 절대 POI id 를 금지한 것과 같은 이유다. 정수 파라미터 2개는 `CompiledStep` 의 `Count` 슬롯 하나에 들어가지 않는다. 존은 `$zone`(현재 존)으로 런타임이 채운다 |
| 2026-07-26 | `docs/01 §5` | `archetypes.json` 에 `initial_inventory` 추가 | T1-27. `docs/11 §8`이 gen_npcs 에게 "초기 인벤토리를 아키타입 기본값으로" 라고 지시하는데 §5 에 그 필드가 없었다. 검증기 3단의 초기 상태(`HasTool` 등)도 여기서 나온다 |
| 2026-07-26 | `docs/03 §3` | 3단의 `ctx.InitialFlags` 에 아키타입 기본 인벤토리 플래그와 도착 시 장소 플래그를 명문화 | T1-27. 버킷 플래그만으로는 `HasTool`·`AtWorkplace` 가 서지 않아 `Work`·`Craft` 를 쓰는 플랜이 전부 3단에서 걸린다 (`docs/03 §1` 예시 포함) |
| 2026-07-26 | `docs/01 §7` | 인터럽트 발동을 엣지 트리거로 명문화 | T1-57. `when` 조건이 대부분 플래그라 이벤트마다 다시 쏘면 `강제 액션 → 완료 이벤트 → 다시 강제` 되먹임이 생긴다. 200마리 1게임일에 강제 액션 155만 건이 나왔다 |
| 2026-07-26 | `docs/01 §4` | POI 출입 제한을 `Workplace`/`Field`/`Wilderness` 로 한정 (`IsWorkSite`) | T1-27·T1-46. `allowed_archetypes` 를 출입 허가로 읽으면 농부가 시장에 못 들어간다 |
| 2026-07-26 | `docs/11 §4` | 인지 스캔에 틱당 상한(`MaxScansPerTick = 150`) + 밴드별 커서 추가 | T1-60. 슬라이스만으로는 상한이 안 지켜진다. LOD 0 은 주기가 1 이라 밴드 전원을 매 틱 본다 — NPC 5,000·플레이어 봇 20 에서 실측 612건 |
| 2026-07-26 | `docs/11 §10` | 대시보드 NPC 패널의 `VisualState` → `StepStatus` | T1-58. VisualState 는 연출이라 게임서버 소관이고 `NpcStore` 에 미러가 없다 |
| 2026-07-26 | `docs/11 §10` | 링크 패널의 이벤트 수신·시퀀스 갭을 `NpcServerLoop` 이 세도록 | T1-58. 루프백 링크는 이벤트 리더를 그대로 넘겨줘서 셀 수 없다. 모든 이벤트가 지나는 곳은 `DrainEvents` 하나뿐이고, 링크마다 넣으면 구현체를 바꿀 때 검출이 사라진다 |
| 2026-07-26 | `README §주요 실행 옵션` · `§프로젝트 구조` | 실행 옵션 7종 추가 · `gen_npcs.csx` → `.cs` | T1-56·T1-57. `--link record 와 replay` 에 경로 인자가 필요했고, 생성기는 dotnet-script 없이 도는 .NET 10 파일 기반 앱이다 |
| 2026-07-26 | `docs/03 §3` | `V3.RESOURCE_IMBALANCE` 를 '자기모순만 잡는다' 로 한정 | T1-27. 3단은 창고 잔량을 모른다. 플랜이 아예 모으지 않는 자원까지 따지면 `docs/03 §1` 예시(석탄을 캐지 않음)가 반려된다 |
| 2026-07-26 | `masterdata/items.json` | `iron_sword` 레시피 입력을 iron_ore 3 → 2 로 | T1-27. `docs/03 §1` 예시가 광석 8개로 검 3자루를 만드는데 3개씩이면 9개가 필요해 사양이 스스로를 반증한다 |
| 2026-07-26 | `docs/11 §5` | `GameClock` 시간 환산 주석 정정 | T1-29. "게임 1분 = 1 tick @ scale 60" 은 같은 문단의 "TimeScale 60이면 실시간 24분" 과 10배 어긋난다. 실제로는 scale 60 에서 1틱 = 6 게임초다 |
| 2026-07-26 | `docs/11 §4` | 인지 스캔의 슬라이스 선택을 `tick % period` 로 정정 | T1-36. `tick % period != 0` 로 거른 뒤 다시 `(tick / period) % period` 로 슬라이스를 고르면 LOD1 의 NPC 가 10틱이 아니라 100틱마다 판정된다. Period 의 뜻과 어긋난다 |
| 2026-07-26 | `docs/03 §5` | `CompiledPlan` 을 `sealed class` → `sealed record` 로 | T1-39. `PlanStore.Register` 가 등록 시점에 `Id` 를 박아야 하는데, 그것 하나 때문에 세터를 열면 런타임 중에 플랜이 바뀔 수 있게 된다. `with` 로 새 인스턴스를 만든다 |
| 2026-07-26 | `docs/01 §10.1` · `CLAUDE.md §2.5` | 프리픽스 예산 4,200~4,500 → **하한 4,096 만 남기고 상한 삭제.** 조립 절을 4개 → 5개로 (아키타입 표 추가) | T2-04. 정식 카탈로그로 조립한 실측이 **11,967 tok** 이다 (`W6_compile_stats.md §1`). 4,409 tok 은 액션 12·플래그 16 축소판 값이었다. 여기에 통과율 처방 셋(액션별 플래그 표·레시피 입출력·아키타입 허용 액션)이 더 붙는데, 서픽스 예산이 300 토큰이라 셋 다 프리픽스에 둘 수밖에 없다 |
| 2026-07-26 | `docs/13 §2` (코드 블록) · `docs/13` T3-01 | `PlanStore` 의 `Publish` → **`SetBucket`** 으로 이름 통일. `_plans` 를 `List<CompiledPlan>` → **청크 배열**(512×128) | T3-01. P1 스텁이 이미 `SetBucket` 을 쓰고 있어 호출부 5곳을 바꿀 이유가 없었다. `List<T>` 는 내부 배열 교체와 `Count` 갱신 사이에 창이 열려 락 없는 읽기가 범위 밖을 볼 수 있다 — 청크는 자라도 기존 청크를 건드리지 않는다 |
| 2026-07-26 | `docs/03 §7` | manifest 에 `file_hashes`·`cache_hit_rate`·`partial`·`generated_by.concurrency` 3필드 추가 | T3-05. `file_hashes` 가 없으면 `docs/13 §3` 의 부분 무효화를 판정할 수 없어 POI 추가마다 전량 재생성이 된다. `cache_hit_rate` 는 P3 게이트가 manifest 에서 읽는 값이고, `partial` 은 `--resume` 이 보는 값이다 |
| 2026-07-26 | `docs/13 §3` | `Compare` 시그니처에 `prefixHash` 인자 추가. 표에 `zones.json`(Partial)·`poi_distances.bin`(None)·미지의 파일(Full) 행 추가. "추가만" 을 코드가 검증하지 않는 이유 명문화 | T3-06. §3 초안의 `cur.Prefix.Sha256` 은 성립하지 않는다 — `MasterDataSet` 은 `PromptPrefix` 를 들고 있지 않고 그 사실이 그 타입 주석에 이미 적혀 있다(`Npc.MasterData → Npc.Llm` 은 CLAUDE.md §3 금지). 파일 해시 하나로는 추가와 수정을 가를 수 없다 |
| 2026-07-26 | `docs/01 §6` 구현 · `README §빠른 시작` | `context_buckets.json` 의 `prebake_priority` 를 **로더가 읽는다** (`BucketSpace.PrebakePriorityOf`). README 의 `--concurrency 32` → `8` | T3-10. §6 이 `prebake_priority` 를 선언해 놓았는데 로더가 읽지 않아 T3-10 의 정렬이 가중치를 코드에 하드코딩할 수밖에 없었다 — CLAUDE.md §2.4 위반이다. 32 는 추정치이고 실측 근거가 없다(`W6_compile_stats.md §6`) |
| 2026-07-26 | `docs/13 §4·§8` · `docs/measurements/W8_prebake.md §1` | 워밍업의 **비용 논거를 실측으로 정정.** "캐시 write 를 32번 지불, 비용 3배" → Gemini 암시적 캐싱에서는 손해가 없다 | T3-11. 실측에서 전제 둘이 다 틀렸다 — 파동 안에서 먼저 도착한 요청이 캐시를 만들어 나머지가 물려받고(동시 32에서 워밍업 없이 68.6% 적중), 암시적 캐싱에는 write 할증이 없다. 동시 32 에서는 워밍업이 10~17% **더 비쌌다.** 그래도 기본으로 켜 두는 이유는 Anthropic 명시적 캐싱과 예비 점검이다 |
| 2026-07-26 | `docs/13 §8` · `docs/measurements/W8_prebake.md §3` | 전수 드라이런 건당 소요 "~50ms"(추정) → **0.02ms**(실측) | T3-15. 드라이런은 10Hz 틱을 굴리지 않고 액션 소요시간만큼 게임시간을 건너뛴다. 2,880건이 병렬도 1 로도 0.04초라 T3-15 의 "미달 시 병렬도를 올리기 전에 건당 소요부터 기록" 조항은 발동하지 않는다 |
| 2026-07-26 | `docs/03 §7` | `planstore/manifest_history.jsonl` 추가 | T3-16. §7 이 "프리베이크를 돌 때마다 보존한다" 고 했는데 `manifest.json` 만 있으면 덮어쓰기라 지난 회차 숫자를 다시 못 본다 |
| 2026-07-26 | `CLAUDE.md §1·§5` · `docs/13 §7` | 테스트 카테고리에 **`Gate`** 추가. CI 기본 필터를 `Category!=Golden&Category!=Gate` 로 | T3-21. 게이트 9항목 중 5개(생성 완료율·wall-clock·실비용·프롬프트 캐시 적중률·검수 채택률)는 실측 산출물이 있어야 판정된다. 산출물이 없을 때 통과로 세면 게이트가 거짓이 되므로 실패시키고, 대신 기본 CI 에서 뺐다. xunit 2.9 에는 `Assert.Skip` 이 없다 |
| 2026-07-26 | `CLAUDE.md §3` · `docs/11 §2` · `docs/12` T2-01 | `Npc.Llm` 의존에 `Npc.MasterData` 추가 | T2-01. 프리픽스는 `actions.json`·`world_flags.json`·`archetypes.json` 에서 **생성**되고(`docs/01 §10.1`, `CLAUDE.md §2.4`), `docs/12 §3` 의 `Build(in PlanRequest, MasterDataSet)` 과 `§6` 의 `_md.Fallbacks[...]` 가 이미 `MasterDataSet` 을 받는다. `Npc.MasterData → Npc.Core` 뿐이라 순환도 없고, `Npc.Runtime ↛ Npc.Llm` 금지는 그대로다 |

#### P2~P5 재검토 (§5 가 예고한 "W1 완료 시점의 재검토")

| 날짜 | 문서 | 변경 | 계기 |
|---|---|---|---|
| 2026-07-26 | `docs/12 §4` · `docs/12` T2-02·T2-09 | 강제 디코딩을 **기본에서 뺀다.** 스키마 위험 요소표를 W1 실측 건수로 교체하고 `minItems/maxItems`·`additionalProperties`·`required`·`maximum` 을 제거 대상으로 | W1 T0-09. full/forced 가 유효 JSON 99/100 인데 어휘 검증 **0/100** 이었다(`V2.UNKNOWN_ARG` 99 — 문법을 지키며 인자를 지어낸다). 같은 모델의 prompt 모드는 93/100. `docs/10 §5` 의 위험 신호("유효율 90% 미만")로는 이 실패가 안 잡힌다 |
| 2026-07-26 | `docs/12 §5` · `docs/12` T2-13·T2-14 | 4단 `DryRunValidator` 를 `Npc.Core` → **`Npc.Sim`** 으로. 계약(`IDryRunValidator`)만 `Npc.Core` 에 남긴다 | `CLAUDE.md §3` 위반이었다. `Npc.Core` 는 `Npc.Contracts` 만 참조하는데 `SimWorld` 는 `Npc.Sim` 에 있다. `IPlanVocabulary`(T1-23) 를 가른 것과 같은 방법 |
| 2026-07-26 | `docs/12 §2·§6` · `docs/12` T2-09·T2-11·T2-15·T2-17 | `Npc.Llm/PlanCompiler` → **`LlmPlanCompiler`** | T1-23 이 이미 `Npc.Core.Plan.PlanCompiler`(`PlanDocument` ↔ `CompiledPlan`)를 만들었고 `Npc.Llm` 은 `Npc.Core` 를 참조한다. 같은 이름을 두면 그 타입이 가려진다 |
| 2026-07-26 | `docs/12 §2` | 일일 하드 캡의 자리를 `Npc.Llm/TokenBudget.cs` → **`Npc.Planning/ReplanBudget.cs`** 로 (`docs/14 §3` 과 일치시킴) | 두 문서가 같은 것을 다른 프로젝트에 두라고 하고 있었다. 예산 판정은 큐에서 뽑기 전에 나야 한다 |
| 2026-07-26 | `docs/12 §3` · `docs/12` T2-04·T2-05 | 서픽스 절 제목을 250 → **300** 토큰(테스트와 일치). 프리픽스 예산 4,200~4,500 은 **재측정 대상**으로 표시하고 하한 4,096 만 절대 조건으로 | W1 프리픽스 4,409 tok 은 **축소판**(액션 12·플래그 16) 값이다. 정식은 액션 37·플래그 42 라 실측 단가(액션 ≈139 tok)로 외삽하면 약 8,000 tok 이다. 캐시에 필요한 것은 하한뿐이므로 상한을 맞추려고 카탈로그를 줄이지 않는다 |
| 2026-07-26 | `docs/13 §2` · `docs/13` T3-01·T3-03 | `PlanStore` 를 `CompiledPlan?[2880]` 단일 배열 → **버킷→PlanId 표 + PlanId→플랜 레지스트리** 두 표로. `NpcStore.PlanId` 주석의 "버킷 인덱스(0..2879)" 를 "레지스트리 id" 로 정정 | 런타임(`PlanExecutor` 4곳·`CognitionScheduler` 1곳)이 이미 `NpcStore.PlanId` 로 조회한다. 단일 배열로는 그 경로가 성립하지 않는다. 인접 버킷 재사용(`docs/12 §7`) 때문에 한 플랜을 여러 버킷이 가리키기도 한다 |
| 2026-07-26 | `docs/13 §4` · `docs/13` T3-12·게이트 · `docs/12` T2-19 | `--concurrency` 기본값 32 → **8**. "동시 32 / 5분" 의 근거를 T2-19 첫 회차 실측으로 이관 | W1 T0-11 은 로컬 2종만 쟀고 외부 API 는 측정하지 않았다. 로컬에서는 429 가 한 번도 나지 않았다. "W1 M4 에서 확정" 이라고 적을 수 있는 값이 없다 |
| 2026-07-26 | `docs/14 §3` · `docs/14` T4-05 | 예산 초기값을 실측 기준으로. T1 초당 요청 0.5 → **0.2**(워커 1 기준). T2 초당 요청·일일 비용 캡은 **미정**으로 표시 | W1 실측 로컬 지연이 4B 3.7s · 8B 5.1s 로 가정(1.75s)의 2~3배다. §5 표의 "지연이 가정의 2배 이상" 행이 발동했다. 외부 API 는 미측정이라 확정할 수 없다 |
| 2026-07-26 | `docs/14 §4` · `docs/14` T4-10·T4-11 | T1 워커 **1개 → 2개**(설정값). "로컬은 배칭이 없어 늘려도 이득 없다" 를 삭제 | W1 T0-11 반증. 동시 2에서 4B 2.46 → 12.07 req/s, 8B → 2.15 req/s 로 오른다. 8B 가 동시 8에서 0.36 으로 붕괴하는 것은 배칭 부재가 아니라 **VRAM 8GB 부족**이다 |
| 2026-07-26 | `docs/14 §2` · `docs/14` T4-01·T4-02 | `ReplanScorer` 가 쓰는 `NpcStore.PlanAssignedTick`·`PendingUrgency` 가 없다는 사실과 콜드 영역 배치를 명문화. 점수 타입 `int` → `float` 의 호출부 3곳을 태스크에 포함 | 태스크 파일 목록에 `NpcStore.cs` 가 없어 그대로 착수하면 컴파일이 안 된다. 핫 배열에 넣으면 `HotBytesPerNpc`(23B) 가 늘어 `docs/11 §3` 의 L2 목표가 깨진다 |
| 2026-07-26 | `docs/14 §5` · `docs/14` T4-13 | `BucketTransition` 의 잔여 작업을 "지터 구현" → **"전환 계기 추가"** 로 정정 | T1-61 이 지터·예약·`Apply`·`PlanSwapper` 결선을 이미 끝냈고 `RequestSwap` 은 버킷 4차원을 다 만든다. 빠진 것은 `Tick` 이 `TimeOfDayChanged` 만 본다는 것 — `ZoneStateChanged`·`WeatherChanged` 에 예약이 안 걸린다 |
| 2026-07-26 | `docs/14 §6` · `docs/14` T4-15 | 부하 매트릭스의 티어 축에 `--tier` 옵션 추가를 명시 (`--no-llm` 은 별칭으로 유지) | `HostOptions` 에 `--no-llm` 하나뿐이라 "T0만 / T0+T1 / T0+T1+T2" 를 가를 수 없다 |
| 2026-07-26 | `docs/14 §6` · `docs/14` T4-16 | 인지 스캔 O(1) 판정은 **상한을 푼 회차로** 해야 한다고 명문화 | `MaxScansPerTick = 150` 이 상한을 걸어 P1 실측이 정확히 150 이었다. 상한이 걸린 상태로 재면 무엇을 넣어도 O(1) 로 보인다 |
| 2026-07-26 | `docs/10` | 게이트 판정 절 신설 (G0-1 미달 · G0-2 미달 · G0-3 통과 · G0-4 판정 불가). T0-09~T0-12 를 `~` 로 | 산출물 4건은 나왔는데 원장은 "미착수" 였다. 규약상 `x` 는 커밋까지이고 아직 워킹 트리에만 있다 |
| 2026-07-26 | `docs/12`~`docs/15` (공통) | 도구 스크립트 6개를 `.csx` → `.cs`(파일 기반 앱). 신규 `.ps1` 4개에 UTF-8 BOM 요구 추가. `tools/Npc.Prebake`·`Npc.Narrate` 의 sln·테스트 참조 배선을 태스크에 포함 | 이 저장소는 dotnet-script 를 안 쓴다(`tools/gen_npcs.cs`). BOM 없는 한국어 `.ps1` 은 PowerShell 5.1 이 ANSI 로 읽어 파서 오류가 난다(`W1_env.md §4.6`). 프로젝트가 sln 밖에 있으면 `dotnet test` 가 완료 조건의 테스트를 아예 안 본다 |
| 2026-07-27 | `docs/15 §2` | 예시 픽스처의 `produces_item_of` 를 `weapon` → **`product`** 로. 단언 표에 "(`items.json` 의 `category`)" 명기 | T5-01. `items.json` 에서 `iron_sword` 의 category 는 `product` 이고 `HasWeapon` 은 grants 다. `weapon` category 는 `hunting_bow`·`axe`·`guard_spear`·`guard_sword` 넷뿐인데 **이 넷에는 레시피가 없다** — 어떤 플랜도 `weapon` 을 산출할 수 없어 예시대로 쓰면 단언이 영구 실패한다 |
| 2026-07-27 | — (코드 버그) | `NullGameServerLink.PushTick` 이 이벤트 시퀀스로 **명령 카운터**(`_enqueued`)를 쓰고 있었다. 시퀀스 필드를 분리 | T5-11. 두 `TickSync` 사이에 나간 명령 수만큼 시퀀스가 건너뛰어 틱 루프의 갭 검출이 계속 경보를 올렸다 — 1,440틱에 **445건**. Null 링크는 "성능 기준선" 이라 아무도 이벤트를 안 봤고, 4종을 나란히 놓기 전에는 드러나지 않았다 |
| 2026-07-27 | `docs/14 §10` · `docs/14` T4-09 | `CircuitBreaker` 를 **T5-10 이 먼저 만들었다**. 시각을 밖에서 받는 형태(`Func<long>`)로. `TieredPlanCompiler` 가 T2 앞에서 `TryEnter` 한다 | T5-10. §4 의 "외부 API 타임아웃 30초 → 서킷 브레이커 개방" 을 판정하려면 브레이커가 있어야 한다. 안에서 시계를 읽으면 "60초 뒤 반개방" 을 테스트하려고 60초를 기다려야 한다. 같이 고친 것 — 라우터의 `catch` 가 `OperationCanceledException` 을 전부 걸러내고 있었는데 **HttpClient 타임아웃도 `TaskCanceledException`** 이라 외부 타임아웃이 워커를 뚫고 나갔다. 호출자가 취소했을 때만 올린다 |
| 2026-07-27 | `docs/15` T5-08 파일 목록 · `docs/02 §5` | KillSwitch 타깃을 문자열 → **열거형 `KillSwitchTarget`(T2·T1·PlanStore)** 로. 상태 객체 `KillSwitchState` 를 `Npc.Core` 에 신설하고 `PlanStore.Switches` 로 결선. `TieredPlanCompiler` 는 **신규**로 만들었다 | T5-08. 선행 T4-07 이 미착수라 "수정" 할 파일이 없었다. 타깃 문자열이 셋(§4 는 `PlanStore` 도 끊는다)이라 열거형이 필요했고, Sim·Llm·Planning 이 같이 보는 상태라 `Npc.Core` 밖에는 둘 자리가 없다(`Npc.Llm ↛ Npc.Sim`). 라우터 없이 기록만 남기면 **안 끊긴 채로 시나리오 C 가 통과해 게이트가 거짓이 된다** |
| 2026-07-27 | `docs/11 §5` (틱 루프) · `docs/15 §3` | 틱 루프에 **틱 경계 규칙 3개**를 명문화. (1) 이벤트 배수는 `TickSync` 에서 끊는다 (2) `FlushAsync` 는 틱마다 한다 (3) 게임서버 대역의 페이싱 기준을 `TicksProcessed` → **`TicksCommitted`** 로 바꾸고, 기다리는 자리를 세계를 민 **뒤 → 앞**으로 옮긴다 | T5-07. §3 은 "명령 jsonl 바이트 동일"을 요구하는데 **기록 자체가 실행마다 달랐다** — 같은 시나리오 2회에서 이벤트 30,416 vs 30,649. 원인 둘: 배수가 틱 경계를 넘어 아직 돌리지 않은 틱의 이벤트를 미리 반영했고, 대역이 명령 flush 전에 다음 틱을 밀었다. 셋을 고치니 기록 2회가 바이트 동일이 되고 재생도 일치한다. 부수 효과로 전체 테스트가 1분 39초 → **42초**로 줄었다 (락스텝 대기에서 `SpinWait` 이 `Thread.Sleep(1)` 로 넘어가지 않게 `sleep1Threshold: -1` 을 준 결과) |

---

## 4. Phase 게이트

각 Phase는 마지막 태스크가 아니라 **게이트 통과**로 끝난다. 게이트는 해당 `docs/1x_Phase*.md`의 "게이트 확인" 절에 있다.

| Phase | 게이트 요약 |
|---|---|
| P0 | 강제 디코딩 100/100 · 지연 가정 ±50% 이내 · 캐시 prefill 70%↓ · 4B가 8B의 80%↑ |
| P1 | **LLM 0회 호출**로 NPC 500마리 게임 7일 완주 · 틱 p99 ≤ 20ms · 마스터데이터 V1~V11 통과 |
| P2 | 검증 통과율 ≥ 90% · 서픽스 ≤ 300 tok · 프리픽스 SHA 1종 · 플랜 다양성 ≥ 60% |
| P3 | 2,880키 콜드 필 ≤ 5분 · 히트율 ≥ 98% · 검수 채택률 ≥ 80% |
| P4 | NPC 5,000에서 히트율 ≥ 98% · 틱 p99 ≤ 20ms · GPU ≤ 60% · 시나리오 B 통과 |
| P5 | 골든 90% · 리플레이 100% 일치 · 시나리오 A/B/C 통과 · 블라인드 평가 n ≥ 480 |

**게이트 미통과 시 다음 Phase로 넘어가지 않는다.** 특히 P0과 P1은 이후 전부의 전제다.

---

## 5. 주의 — 이 계획의 유효기간

**P0(W1)의 실측 결과가 P2 이후 태스크를 바꾼다.** 아래는 착수 전에 세워 둔 분기표이고, **오른쪽 두 열이 2026-07-26 재검토 결과다.**

| W1에서 나올 수 있는 결과 | 영향받는 태스크 | 실제 | 처리 |
|---|---|---|---|
| 4B가 8B와 품질이 같다 | T4-05 예산 상한 2배 상향, T3-12 동시성 재설정 | **판정 불가** — 8B 미채점 | 로컬 모델 미확정. T0-12 재실행 대기 |
| 강제 디코딩이 95% 미만 | T2-02 스키마 단순화, T2-23(count 심볼화)을 필수로 승격 | **분기 자체가 틀렸다** — 유효 JSON 은 99% 인데 어휘 검증이 0% | T2-02 단순화 반영 + **강제 디코딩을 기본에서 제거.** T2-23 은 3단을 안 돌려 봐서 여전히 조건부 |
| 캐시 적중률이 낮다 | T2-04 프리픽스 길이 재설계, T3-11 워밍업 전략 변경 | **아니다** — prefill 85~88% 감소 | 워밍업 전략 유지. 단, 프리픽스 길이는 **다른 이유**로 재설계한다(축소판 12액션 → 정식 37액션) |
| 플랜 다양성이 낮다 | T2-21 개선 루프를 L 규모로 확대, 아키타입 40 → 25 축소 검토 | **낮다** — 4B 유니크 시퀀스 ≈ 58% (목표 60%) | T2-22 에 예고로 기재. 아키타입 축소는 T2-19 전량 생성 실측을 보고 판단 |
| 지연이 가정의 2배 이상 | 상위 계획 §2.1/§10 전면 갱신, P4 예산 재계산 | **2~3배** — 4B 3.7s · 8B 5.1s | P4 예산(`docs/14 §3`) 재계산 **완료.** 상위 계획·`CLAUDE.md §9` 갱신은 **T0-13 에 남아 있다** |

**재검토는 2026-07-26 에 했고 결과는 §3 의 "P2~P5 재검토" 표에 있다.** P2~P5 태스크 문서는 이미 위 실측을 반영해 고쳐 뒀다.

남은 것은 **T0-13** 하나다 — `W1_results.md` 의 M1~M6 표와 5개 결정, 그리고 상위 계획·`CLAUDE.md §9` 의 배경 수치 갱신.
그때까지 P2~P5 문서는 M 번호 대신 원본 측정 파일(`W1_schema.md`·`W1_perf.csv`·`W1_concurrency.md`·`W1_env.md`)을 직접 인용한다.
