# AGENTS.md

이 저장소에서 일하는 코딩 에이전트를 위한 지침. **`CLAUDE.md` 가 본문이고 이 파일은 그 요약이 아니다** —
여기 있는 것은 에이전트가 특히 자주 어기는 것들이다.

- 규칙 전문: **`CLAUDE.md`** (틱 루프 · 계약 N1~N8 · 결정론 · 마스터데이터 · 프롬프트 · 의존 그래프)
- 작업별 파일 지도: **`CODEMAP.md`**
- 작업 지시서: **`PRODUCTION_ROADMAP.md`**
- LLM 온보딩 팩: **`docs/llm/`** (`CONTEXT.md` 부터 읽는다)
- MCP 가 붙어 있으면 파일을 뒤지기 전에 **툴을 부른다** (`.mcp.json` · E-03)

---

## 1. 시작하기 전에

1. `CODEMAP.md` 에서 해당 작업 줄을 찾는다 → **거기 적힌 파일만** 연다.
   `src` 96파일 21,000줄을 매번 훑지 않는다.
2. 고칠 파일의 `tests/Npc.Tests/<같은이름>Tests.cs` 를 먼저 읽는다 — 무엇을 보장하는지가 거기 있다.
3. `CLAUDE.md` §2 의 절대 규칙에 걸리는지 본다.

## 2. 끝내기 전에

```powershell
dotnet build -c Release                        # 경고 0 · 오류 0 (TreatWarningsAsErrors)
dotnet test "--filter Category!=Golden&Category!=Gate&Category!=Load"
dotnet format --verify-no-changes
dotnet run --project tools/Npc.Cli -- validate # 마스터데이터를 건드렸다면
```

- 문서와 어긋나는 변경이면 **같은 커밋에서** 해당 레퍼런스 HTML 도 고친다.
- `working_log.md` 에 항목을 더한다. 시각은 **실제 명령으로 확인한 KST** 다.
- 커밋 메시지는 한국어 `<scope>: <내용>`. **변경 하나 = 커밋 하나.**

## 3. 특히 자주 어기는 것

| 하면 안 되는 것 | 왜 |
|---|---|
| 틱 루프에 LINQ·`new`·문자열 보간 한 줄 | 틱당 할당이 0 이어야 한다. 계기는 `/metrics` 의 `bytesPerTick` 하나다 |
| `code`·`bit` 번호 재배치 | 프리베이크된 플랜 2,880개가 통째로 깨진다. 추가는 **뒤에만** |
| 액션·플래그·아키타입을 코드에 하드코딩 | `masterdata/` 가 단일 원천이다 |
| 게임 로직에 `DateTime.Now`·무시드 `Random` | 리플레이 100% 일치가 게이트다. 시간은 `Tick`, 난수는 `(npcId, tick)` 해시 |
| 플레이어가 쓴 문자열을 프롬프트에 | 프롬프트 인젝션. 구조화 enum 과 내부 ID 만 |
| 경고를 `#pragma warning disable` 로 덮기 | 고친다 |
| 검증을 건너뛰고 플랜을 올리기 | 4단 검증을 통과하지 않은 플랜은 런타임에 올리지 않는다 |
| "미측정"·"미실시" 표시를 지우기 | 없는 것을 채우면 그 문서를 근거로 쓴 판단이 전부 거짓이 된다 |

## 4. 모르겠으면

근거가 불충분하거나 문서와 코드가 어긋나면 **임의로 코드에 맞추지 말고 멈추고 보고한다.**
이 저장소는 어긋난 값을 어긋난 대로 적는 쪽을 택한다.

---

## 5. 테스트
### 5.1 테스트를 쓸까 말까 — 판단 기준 하나

**"이 테스트를 지우면 어떤 현실적인 결함을 놓치는가."** 답이 안 나오면 쓰지 않고, 이미 있으면 지운다.

**테스트 건수·커버리지를 목적으로 삼지 않는다.** 둘 다 지표이지 목표가 아니다.
1,500개 중 300개가 아무것도 안 잡으면, 그 300개는 자산이 아니라 **매번 갚는 이자**다 —
실행 시간, 깨질 때의 조사 시간, 리팩터링할 때의 수정 부담.

#### 지운다

| 유형 | 무엇 | 왜 |
|---|---|---|
| **구현의 복사본** | 프로덕션 로직을 테스트가 다시 계산해 견준다 | 둘이 같이 틀리면 통과한다. 틀린 계산을 고정할 뿐이다 |
| **상수 되읽기** | `Assert.Equal(150, CognitionScheduler.MaxScansPerTick)` | 값을 두 곳에 적은 것이다. 바꾸면 둘 다 바뀌므로 아무것도 못 막는다. **그 상수가 만드는 동작**을 본다 |
| **개수 단언** | `Assert.Equal(37, data.Actions.Count)` | 콘텐츠를 더할 때마다 깨지고, 깨져도 결함이 아니다. 실제로 F-05 는 이 병을 아키타입에서 없앴다 |
| **타입·라이브러리 보증** | enum 값이 0인지, `record` 가 값 비교를 하는지, BCL 이 정렬을 하는지 | 컴파일러와 BCL 이 이미 보장한다 |
| **모킹의 자체 검증** | 가짜가 부른 횟수·순서를 단언한다 | 가짜의 동작을 고정한 것이지 프로덕션을 본 것이 아니다. **관찰 가능한 결과**를 본다 |
| **중복** | 같은 불변식을 여러 파일이 각자 단언한다 | 하나가 깨지면 전부 깨진다. 소음이 N배가 되고 고칠 곳은 하나다 |
| **내부 구현 의존** | private 필드·호출 순서·중간 자료구조를 본다 | 리팩터링마다 빨간불이다. 계약이 아닌 것을 계약으로 만든다 |
| **과도한 케이스 분기** | 같은 분기를 도는 `InlineData` 6줄 | 대표 1건 + **기제가 다른** 1건이면 같은 것을 지킨다 |

#### 남긴다

- **경계·전이** — 상한 ±1, 빈 입력, 되감김, 겹침
- **불변식** — "절대 null 을 안 준다", "번호가 재배치되지 않았다", "패킷에 `string` 이 없다"
- **드리프트** — 손으로 둘 이상 유지하는 것(계약↔와이어 DTO↔C++ 헤더↔파이썬 코덱, 코드↔생성 문서)
- **검사기의 자체 시험** — 스캐너·채점기는 **아무것도 못 찾는 상태로도 통과한다**. 대조군으로 "실제로 잡는가"를 한 번 본다
- **회귀** — 실제로 난 사고를 고정한 것. 주석에 **무슨 사고였는지** 적는다

#### 고칠 때

- 테스트가 깨지면 먼저 묻는다: **계약이 바뀐 것인가, 구현이 새어 나온 것인가.**
  전자면 테스트를 고치고, 후자면 그 테스트는 애초에 내부를 보고 있었다 — 지울 후보다
- **불안정한 테스트는 즉시 고치거나 지운다.** 가끔 빨간불인 테스트는 사람이 전체 결과를 안 믿게 만든다.
  이 저장소의 시간 의존 테스트(`Task.Delay`·`WaitAsync`)가 그 후보다
- 테스트를 지우는 커밋은 **무엇을 더 이상 지키지 않는지**를 커밋 메시지에 적는다
